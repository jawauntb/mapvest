{
  "info": {
    "name": "Mapvest API — v0.1.0-alpha.0",
    "description": "Mapvest turns places and objects into investable tickers. All request and response shapes are generated from zod schemas in `packages/core/src/schemas` — never hand-edit `openapi.yaml`.\n\nGenerated from openapi.yaml. Regenerate: `bun run openapi && bun run postman`.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "system",
      "description": "Health + system endpoints",
      "item": [
        {
          "name": "Health check",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/health",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "health"
              ]
            },
            "description": "Liveness probe. Returns 200 while the API is accepting traffic."
          }
        }
      ]
    },
    {
      "name": "identify",
      "description": "Photo → investable identification",
      "item": [
        {
          "name": "Identify investable brands from a photo",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/identify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "identify"
              ]
            },
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "image",
                  "type": "file"
                },
                {
                  "key": "location",
                  "type": "text",
                  "value": "{\"lat\":-90,\"lng\":-180}"
                }
              ]
            },
            "description": "Accepts a multipart image plus optional location. Runs the vision pipeline and returns detected brands with ticker / comparable / ETF annotations. See `packages/vision`.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Finds journal for the signed-in user",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/finds?limit=100",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "finds"
              ],
              "query": [
                {
                  "key": "limit",
                  "value": "100",
                  "description": "Max finds to return. Default 100, capped at 200."
                }
              ]
            },
            "description": "Every successful `/v1/identify` by a signed-in user records the top investable as a find. Returns finds newest-first.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Progression (XP, level, streak) for the signed-in user",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/progress",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "progress"
              ]
            },
            "description": "Server-side truth for XP, level, streak length, streak-freeze inventory, and earned `badges`. Written by `recordFind` on every successful `/v1/identify`; the client renders this rather than deriving a streak locally, so the streak survives reinstall. `lastFindDay` is a UTC calendar day (`YYYY-MM-DD`). `badges` holds opaque earned badge keys (e.g. `sector:Consumer Staples`) awarded server-side — clients never post a badge and must render an unknown key generically.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Counterfactual universe portfolio",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/universe/summary",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "universe",
                "summary"
              ]
            },
            "description": "The `$100 per find at found price` aggregate: what the caller's universe would be worth if $100 had gone into every find at the moment it was found. Computed from each find's `foundPrice` against the current quote. Finds without a `foundPrice` are excluded from the valued totals, never estimated (AGENTS.md §2.4). Hypothetical only — not a holdings statement. `sources` cites each quote provider that actually declared itself, with confidence derived from the quote's own freshness declaration (AGENTS.md §6 — a quote reporting no provider adds no citation rather than a fabricated one).",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Collection progress (sector dexes + regional tiles)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/dex",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "dex"
              ]
            },
            "description": "Derived on read by reconciling the caller's finds against the `brands.json` seed in `packages/finance`: per-sector found/total counts, plus `tilesVisited` (distinct geohash-6 tiles with at least one find) and `rarityCounts`, the per-find histogram over the four `DexRarity` tiers. Every find is classified into exactly one tier, so the four counts sum to `totalFinds`.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Daily quests for the signed-in user",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/quests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "quests"
              ]
            },
            "description": "The caller's quest set for the current UTC day. Quests cover verifiable actions only (`catch_any`, `catch_private`, `new_tile`, `new_sector`) and are evaluated server-side from the find stream — completion is never self-reported, so there is no endpoint to claim one. `id` is deterministic per day and kind (`{YYYY-MM-DD}:{kind}`), which is what makes XP grantable at most once per quest. `xpGrantedToday` reports XP already written to `user_progress` for `day`; the client displays it and never adds to it.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Tile completion for a location",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/territory?lat=40.7128&lng=-74.006",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "territory"
              ],
              "query": [
                {
                  "key": "lat",
                  "value": "40.7128"
                },
                {
                  "key": "lng",
                  "value": "-74.006"
                }
              ]
            },
            "description": "Completion state for the geohash-6 tile containing `lat`/`lng`. `investablesTotal` is how many investable brands the same nearby cascade behind `/v1/nearby` resolves inside the tile, and `found` is how many of those the caller has already caught, so the two reconcile with `/v1/nearby` for that tile. `pioneer` is true when the caller has not yet recorded a find in this tile — the write-time pioneer XP bonus is still available here. The counts come from a live places + brand join, so the response carries `sources` (AGENTS.md §6) — an uncitable lookup returns fewer sources, never an invented one.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Currently open XP event, if any",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/events/current",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "events",
                "current"
              ]
            },
            "description": "The scheduler-driven quest modifier that is open right now — e.g. Sector Saturday doubling XP for one sector, or an earnings-week window. The schedule is global rather than per-user, so no auth is required. `active` is explicitly `null` when no window is open, letting the client distinguish 'no event' from a failed fetch. The `multiplier` applies to find XP only (quest XP is awarded at face value), only inside `[startsAt, endsAt)`, and an event with no `sector` applies to every sector."
          }
        },
        {
          "name": "Weekly matchups for the signed-in user",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/rivalries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "rivalries"
              ]
            },
            "description": "The caller's tracked solo matchups (e.g. NVDA vs AMD) with the running win/loss/draw record. Collection and comprehension mechanic only: a rivalry is not a position and the copy never instructs a trade. `weekStart` is the Monday of the current round as a UTC calendar day, so the round boundary does not move with the device timezone.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Start a weekly matchup against a comparable",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/rivalries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "rivalries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ticker\": \"\",\n  \"rivalTicker\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Creates a solo weekly matchup for one of the caller's finds. Omit `rivalTicker` and the server picks the opponent from the existing comparables pipeline in `packages/finance` — the client must not invent a rival, and a ticker with no resolvable comparable returns 422 rather than a fabricated opponent. The optional pre-registered `currentPick` on the returned rivalry is a conviction game scored for XP; it is not an order and creates no position.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Pre-register (or clear) the conviction pick for the open round",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/rivalries/{id}/pick",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "rivalries",
                "{id}",
                "pick"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pick\": \"ticker\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Body is `{ pick: \"ticker\" | \"rival\" | null }` — `null` clears the pick. A correct pick earns XP at the weekly close, which then clears it so a pick never carries into a round it was not registered for. Not an order; creates no position.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Delete a rivalry",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/rivalries/{id}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "rivalries",
                "{id}"
              ]
            },
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "nearby",
      "description": "Location-driven place lookup",
      "item": [
        {
          "name": "Nearby investable places",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/nearby?lat=0&lng=0&radius=500&limit=25",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "nearby"
              ],
              "query": [
                {
                  "key": "lat",
                  "value": "0"
                },
                {
                  "key": "lng",
                  "value": "0"
                },
                {
                  "key": "radius",
                  "value": "500"
                },
                {
                  "key": "limit",
                  "value": "25"
                }
              ]
            },
            "description": "Google Places lookup around a lat/lng, filtered and annotated with tickers/ETFs. Every finance answer includes a `sources: Source[]` array (see AGENTS.md §6).",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "widget",
      "description": "Home-screen widget data (iOS WidgetKit / Android App Widget)",
      "item": [
        {
          "name": "Trimmed nearby payload for home-screen widgets",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/widget/nearby?lat=0&lng=0&radius=500&limit=25",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "widget",
                "nearby"
              ],
              "query": [
                {
                  "key": "lat",
                  "value": "0"
                },
                {
                  "key": "lng",
                  "value": "0"
                },
                {
                  "key": "radius",
                  "value": "500"
                },
                {
                  "key": "limit",
                  "value": "25"
                }
              ]
            },
            "description": "Same places cascade + brand join as `/v1/nearby`, capped small (max 12) with quotes attached for up to 6 tickers. Built for the iOS WidgetKit and Android home-screen widgets, which refresh on a timeline rather than on demand. No auth required."
          }
        },
        {
          "name": "Static map PNG for home-screen widgets",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/widget/map-snapshot?lat=0&lng=0&radius=500&limit=25&width=0&height=0",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "widget",
                "map-snapshot"
              ],
              "query": [
                {
                  "key": "lat",
                  "value": "0"
                },
                {
                  "key": "lng",
                  "value": "0"
                },
                {
                  "key": "radius",
                  "value": "500"
                },
                {
                  "key": "limit",
                  "value": "25"
                },
                {
                  "key": "width",
                  "value": "0",
                  "description": "PNG width in px, capped at 640."
                },
                {
                  "key": "height",
                  "value": "0",
                  "description": "PNG height in px, capped at 640."
                }
              ]
            },
            "description": "Server-rendered Google Static Maps PNG with a pin for the origin and one per nearby investable, labeled by ticker. Proxies the request so `GOOGLE_MAPS_API_KEY` never reaches a widget extension (see docs/SECRETS.md). Returns 501 when no key is configured."
          }
        }
      ]
    },
    {
      "name": "finance",
      "description": "Ticker / comparable / ETF resolution",
      "item": [
        {
          "name": "Resolve a private brand to a public comparable + ETF exposure",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/resolve-comparable",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "resolve-comparable"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"brand\": \"\",\n  \"hintSector\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Given a brand name, returns the closest public comparable, ETF exposures, and cited sources. Confidence is `low` when the pipeline could not cite a source (do not fabricate).",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Daily provider-routed price history",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/quote-history?symbol=AAPL&period=1mo",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "quote-history"
              ],
              "query": [
                {
                  "key": "symbol",
                  "value": "AAPL"
                },
                {
                  "key": "period",
                  "value": "1mo"
                }
              ]
            },
            "description": "Daily adjusted closes from the configured market-data provider for the native Overview price series. Period defaults to `1mo`. Does not invent prices — 502 when history is unavailable."
          }
        },
        {
          "name": "Underlying Analyzer chart image",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/chart/{type}?ticker=MCD&period=1mo&month=0",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "chart",
                "{type}"
              ],
              "query": [
                {
                  "key": "ticker",
                  "value": "MCD"
                },
                {
                  "key": "period",
                  "value": "1mo"
                },
                {
                  "key": "month",
                  "value": "0"
                }
              ]
            },
            "description": "Proxies `POST /api/charts/{type}` on Underlying Analyzer. Period aliases `1m`/`1M` normalize to the sibling analyzer's `1mo` period."
          }
        },
        {
          "name": "Market-data provider capabilities",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/market-data/capabilities",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "market-data",
                "capabilities"
              ]
            },
            "description": "Returns the configured primary provider, freshness declaration, subscription labels, and dataset capability notes. The subscription labels are operational configuration and are not inferred from API responses."
          }
        },
        {
          "name": "Historical market aggregates",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/market-data/aggregates?symbol=AAPL&from=2026-01-01&to=2026-01-31&multiplier=1&timespan=day&adjusted=false&assetClass=stocks",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "market-data",
                "aggregates"
              ],
              "query": [
                {
                  "key": "symbol",
                  "value": "AAPL"
                },
                {
                  "key": "from",
                  "value": "2026-01-01"
                },
                {
                  "key": "to",
                  "value": "2026-01-31"
                },
                {
                  "key": "multiplier",
                  "value": "1"
                },
                {
                  "key": "timespan",
                  "value": "day"
                },
                {
                  "key": "adjusted",
                  "value": "false"
                },
                {
                  "key": "assetClass",
                  "value": "stocks"
                }
              ]
            },
            "description": "Additive OHLCV aggregate endpoint backed by Massive when configured. Dates are YYYY-MM-DD; timestamps in points are Unix seconds. The endpoint returns an opaque `nextCursor` when the provider has another page."
          }
        },
        {
          "name": "Financial ratios",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/financials/ratios?ticker=&limit=1&cursor=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "financials",
                "ratios"
              ],
              "query": [
                {
                  "key": "ticker",
                  "value": ""
                },
                {
                  "key": "limit",
                  "value": "1"
                },
                {
                  "key": "cursor",
                  "value": ""
                }
              ]
            },
            "description": "Additive financial-ratios page backed by the configured market-data provider. Continuation cursors are opaque and must be passed back unchanged."
          }
        },
        {
          "name": "Option contract summary",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/options/summary?underlying=&contract=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "options",
                "summary"
              ],
              "query": [
                {
                  "key": "underlying",
                  "value": ""
                },
                {
                  "key": "contract",
                  "value": ""
                }
              ]
            },
            "description": "Additive single-contract snapshot backed by Massive options data, including normalized contract details, greeks, quote, trade, and day fields when available."
          }
        },
        {
          "name": "Option contract bars",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/options/bars?ticker=&from=&to=&multiplier=1&timespan=day&adjusted=true&cursor=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "options",
                "bars"
              ],
              "query": [
                {
                  "key": "ticker",
                  "value": ""
                },
                {
                  "key": "from",
                  "value": ""
                },
                {
                  "key": "to",
                  "value": ""
                },
                {
                  "key": "multiplier",
                  "value": "1"
                },
                {
                  "key": "timespan",
                  "value": "day"
                },
                {
                  "key": "adjusted",
                  "value": "true"
                },
                {
                  "key": "cursor",
                  "value": ""
                }
              ]
            },
            "description": "Additive OHLCV bars for an options contract. The provider continuation cursor is opaque and must be passed back unchanged."
          }
        },
        {
          "name": "Options chain snapshot",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/options/chain?underlying=AAPL&expiration_date=&contract_type=call&strike_price=0&limit=1&cursor=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "options",
                "chain"
              ],
              "query": [
                {
                  "key": "underlying",
                  "value": "AAPL"
                },
                {
                  "key": "expiration_date",
                  "value": ""
                },
                {
                  "key": "contract_type",
                  "value": "call"
                },
                {
                  "key": "strike_price",
                  "value": "0"
                },
                {
                  "key": "limit",
                  "value": "1"
                },
                {
                  "key": "cursor",
                  "value": ""
                }
              ]
            },
            "description": "Additive option-chain snapshot with Massive-normalized contracts, greeks, implied volatility, quotes, trades, and open interest when the subscribed Options plan includes them."
          }
        },
        {
          "name": "Options contract reference data",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/options/contracts?underlying=&ticker=&expiration_date=&contract_type=call&expired=false&as_of=&strike_price=0&limit=1&cursor=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "options",
                "contracts"
              ],
              "query": [
                {
                  "key": "underlying",
                  "value": ""
                },
                {
                  "key": "ticker",
                  "value": ""
                },
                {
                  "key": "expiration_date",
                  "value": ""
                },
                {
                  "key": "contract_type",
                  "value": "call"
                },
                {
                  "key": "expired",
                  "value": "false"
                },
                {
                  "key": "as_of",
                  "value": ""
                },
                {
                  "key": "strike_price",
                  "value": "0"
                },
                {
                  "key": "limit",
                  "value": "1"
                },
                {
                  "key": "cursor",
                  "value": ""
                }
              ]
            },
            "description": "Additive, cursor-paginated options contract index. Massive exposes active and expired contracts with expiration dates, strikes, types, and exercise styles."
          }
        },
        {
          "name": "Single options contract",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/options/contracts/{ticker}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "options",
                "contracts",
                "{ticker}"
              ]
            },
            "description": "Additive options contract reference lookup by Massive options ticker."
          }
        },
        {
          "name": "Corporate actions and market events",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/market-events?ticker=&from=&to=&limit=0",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "market-events"
              ],
              "query": [
                {
                  "key": "ticker",
                  "value": ""
                },
                {
                  "key": "from",
                  "value": ""
                },
                {
                  "key": "to",
                  "value": ""
                },
                {
                  "key": "limit",
                  "value": "0"
                }
              ]
            },
            "description": "Returns Massive stock splits and dividends, plus optional TMX/Wall Street Horizon corporate events when the partner dataset is enabled."
          }
        },
        {
          "name": "Underlying Analyzer snapshot",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/analysis/{ticker}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "analysis",
                "{ticker}"
              ]
            },
            "description": "Lightweight stock summary (+ brief when present). Full Anthropic brief via POST /v1/memo."
          }
        },
        {
          "name": "Company value-chain graph",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/graph/{ticker}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "graph",
                "{ticker}"
              ]
            },
            "description": "Cache-first: served from the `company_edges` store, extracted on miss with in-flight dedupe per ticker and refreshed when a new 10-K lands rather than on a short TTL. Cache hits are free and identity-less; a cache MISS spends provider money, so generation requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent; concurrent callers sharing one in-flight extraction are charged once). Edges are cited from 10-K items 1/1A evidence (supplier concentration and >10% customers are disclosed there) plus Exa open-web results; every edge carries `sources` (AGENTS.md §6) and an edge that cannot be cited is not emitted. Private counterparties keep `dstName` with no `dstTicker` — never an invented one. Empty and failing extractions are negatively cached for an hour. Returns 502 when extraction fails."
          }
        },
        {
          "name": "Demand pulse (is the money upstream growing or shrinking)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/pulse/{ticker}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "pulse",
                "{ticker}"
              ]
            },
            "description": "Aggregates the buyer side of a ticker's value chain into one signal: the `buys_from` counterparties from `/v1/graph/{ticker}` joined to provider income-statement / cash-flow series, weighted by edge weight. Metered exactly like `/v1/graph` — cache hits are free and identity-less; a cache MISS spends provider money, so generation requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent). `pulse` is `null` (and `interpretation` is `unknown`) when no buyer fundamentals resolved — never zero-filled — and a buyer whose series is missing keeps `revenueYoY`/`capexYoY` omitted (AGENTS.md §2.4). Every fetched series is cited in `sources` (AGENTS.md §6)."
          }
        },
        {
          "name": "Macro environment brief for a sector",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/environment/{sector}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "environment",
                "{sector}"
              ]
            },
            "description": "The gather → LLM → tailwinds/headwinds brief at sector scale, cached 24h. `series` holds the quantitative FRED observations behind the brief; policy and culture color gathered via Exa with recency filters is qualitative and is cited in `sources` alongside them, never promoted into `series`. Metered exactly like `/v1/graph` — cache hits are free, a cache MISS requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent). Degrades honestly: with `FRED_API_KEY` unset the brief ships with `series: []` and Exa color only; 503 only when neither FRED nor Exa (nor the LLM) is configured. Macro numbers are never synthesized."
          }
        },
        {
          "name": "Layered synthesis memo (billable)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/memo/synthesis",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "memo",
                "synthesis"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ticker\": \"NVDA\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "The layered memo. The prompt receives the three layer briefs — upstream (`/v1/graph/{ticker}`), demand (`/v1/pulse/{ticker}`), and environment (`/v1/environment/{sector}`) — plus ratios, and is asked exactly three questions: what is the binding constraint on this business, how durable is the demand above it, and where in the chain does pricing power sit. Those answers come back as `bindingConstraint` / `demandDurability` / `pricingPower`, each grounded in the cited layer briefs. They are optional because the memo degrades gracefully: with an empty graph, no pulse, and no environment brief it returns a plain `memo` and omits the layer answers rather than guessing them. Metered exactly like `/v1/graph` — cache hits are free, a cache MISS requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent). Every layer fact carried into an answer is cited in `sources` (AGENTS.md §6)."
          }
        },
        {
          "name": "Watchlist cockpit (Underlying Analyzer)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/cockpit",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "cockpit"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tickers\": [\n    \"\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Batch rank for up to 10 tickers via POST /api/watchlists/cockpit."
          }
        },
        {
          "name": "Watchlist alerts digest (Underlying Analyzer)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/alerts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "alerts"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tickers\": [\n    \"\"\n  ],\n  \"maxAlerts\": 0\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Alert digest for up to 10 tickers via POST /api/watchlists/alerts."
          }
        },
        {
          "name": "Start or continue a durable research conversation",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/agent/chat",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "agent",
                "chat"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"message\": \"\",\n  \"ticker\": \"\",\n  \"conversationId\": \"\",\n  \"threadId\": \"\",\n  \"clientMessageId\": \"\",\n  \"researchDepth\": \"auto\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Uses Derivation Research Console /api/explore in agent mode, then returns its display projection as an article-shaped turn. Follow-ups reuse conversationId and one clientMessageId per user message. Broker orders stay disabled."
          }
        },
        {
          "name": "List persisted research brief threads",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/agent/threads",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "agent",
                "threads"
              ]
            },
            "description": "Lists the caller's Mapvest-owned durable conversation references."
          }
        },
        {
          "name": "Get lightweight durable research status",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/agent/threads/{id}/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "agent",
                "threads",
                "{id}",
                "status"
              ]
            },
            "description": "Uses Derivation GET /api/autoresearch?summary=1 for an owned conversation."
          }
        },
        {
          "name": "Download a completed research memo",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/agent/threads/{id}/memo",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "agent",
                "threads",
                "{id}",
                "memo"
              ]
            },
            "description": "Owner-scoped PDF proxy for the durable conversation memo; the Console service credential remains server-only."
          }
        },
        {
          "name": "Get one research brief thread",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/agent/threads/{id}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "agent",
                "threads",
                "{id}"
              ]
            },
            "description": "Uses Derivation GET /api/autoresearch?display=1 for an owned conversation."
          }
        },
        {
          "name": "Generate a ticker memo (billable)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/memo",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "memo"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ticker\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Full research memo for a ticker. Counts against the 50-generation free tier. Returns 402 `quota_exceeded` when the meter is spent."
          }
        }
      ]
    },
    {
      "name": "push",
      "description": "Device push-token registration and revocation",
      "item": [
        {
          "name": "Register or re-claim this device for push notifications",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/push/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "register"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\",\n  \"platform\": \"ios\",\n  \"deviceId\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Registers the physical Expo token to the signed-in account. A token already owned by this account is idempotent; registration under another account transfers ownership and starts the new claim muted, so notification consent never crosses accounts.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Read preferences for the current device",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/push/prefs?tokenId=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "prefs"
              ],
              "query": [
                {
                  "key": "tokenId",
                  "value": ""
                }
              ]
            },
            "description": "Returns preferences only for the exact signed-in installation named by `tokenId`. Omit `tokenId`, or provide an unknown or other-user id, to receive an explicit empty result with `tokenId: null`; the API never selects another device as a fallback.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Merge preferences for one registered device",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/push/prefs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "prefs"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tokenId\": \"\",\n  \"prefs\": {\n    \"notifications_enabled\": false,\n    \"daily_brief\": false,\n    \"local_brief\": false,\n    \"price_alerts\": false,\n    \"memo_finished\": false,\n    \"agent_response\": false,\n    \"identify_done\": false,\n    \"watchlist_mover\": false,\n    \"find_evolution\": false,\n    \"uncaught_nearby\": false,\n    \"last_lat\": 0,\n    \"last_lng\": 0,\n    \"last_location_at\": \"\"\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Updates only the signed-in user's matching installation. Event opt-ins and the product-level notification switch are per device; unknown patch fields are accepted and ignored for forward-compatible clients.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Unregister one signed-in device",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/push/token/{id}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "token",
                "{id}"
              ]
            },
            "description": "Deletes only the supplied opaque registration id when it belongs to the signed-in user, tombstoning its physical claim and removing future delivery eligibility.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Revoke this device with its opaque push token id",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/push/revoke-device",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "revoke-device"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\",\n  \"tokenId\": \"\",\n  \"deviceId\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Public, idempotent sign-out fallback. It requires both the physical Expo token and the opaque id returned by registration, so a stale client cannot revoke a later account owner. `already-revoked` accepts a retry after an earlier unlink; `claim-mismatch` is fail-closed because another active owner now holds this physical token."
          }
        },
        {
          "name": "Revoke a former session's current device during expired-session recovery",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/push/revoke-expired-session-device",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "revoke-expired-session-device"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\",\n  \"tokenId\": \"\",\n  \"deviceId\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Narrow recovery for a cryptographically valid session JWT. Expo-token-only recovery is limited to the 90 days after expiry; its historical opaque token id may be used without that age limit when iOS cannot obtain an Expo token. It verifies the signature, HS256 algorithm, session purpose, and subject without accepting the JWT for normal authentication; the signed subject plus either identity may only revoke that same user's active claim. It returns no user data and fails closed with `claim-mismatch` if another owner claimed the physical token.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Revoke the signed-in user's current device without a stored token id",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/push/revoke-current-device",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "push",
                "revoke-current-device"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\",\n  \"deviceId\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Authenticated recovery for a valid session that lost its local opaque push token id. The current user plus Expo identity can revoke only that user's active claim; expired sessions without the opaque id must use the public fallback and fail closed on `claim-mismatch`.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "auth",
      "description": "Passwordless email sign-in",
      "item": [
        {
          "name": "Passwordless email sign-in (magic link)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/auth/session",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "auth",
                "session"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"user@example.com\",\n  \"code\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Starts or completes a magic-link session. On first call with `{email}`, an email is dispatched. On second call with `{email, code}`, a session token is returned."
          }
        }
      ]
    },
    {
      "name": "billing",
      "description": "Entitlements + $19.99/mo subscription checkout",
      "item": [
        {
          "name": "Free-tier remaining + plan",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/entitlements",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "entitlements"
              ]
            },
            "description": "Auth optional. Anonymous callers send `X-Device-Id` so remaining generations can be tracked per device. Mirrors the state `requireGenerationQuota` consumes."
          }
        },
        {
          "name": "Start a $19.99/mo subscription",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/billing/checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "billing",
                "checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"platform\": \"web\",\n  \"successUrl\": \"\",\n  \"cancelUrl\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Signed-in only. `platform` selects the charge channel: Stripe Checkout URL for web (and iOS/Android until native product ids are configured), or `apple_iap` / `google_play` product ids once those env vars are set. Clients must not invent payment URLs.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Stripe customer portal (manage / cancel)",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/billing/portal",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "billing",
                "portal"
              ]
            },
            "description": "Signed-in Stripe subscribers only. Native-store subscribers manage billing in App Store / Play, not here.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Redeem a StoreKit 2 transaction",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/v1/billing/apple",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "billing",
                "apple"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"signedTransaction\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Signed-in only. Body is the JWS from StoreKit (`purchase.purchaseToken` on iOS). The API verifies Apple's ES256 + x5c chain against Apple Root CA G3, then sets `plan=subscribed`. Native subscribers manage billing in the App Store, not Stripe portal.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "admin",
      "description": "Requires the `admin` scope",
      "item": [
        {
          "name": "Admin: request + cost metrics",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/admin/metrics",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "admin",
                "metrics"
              ]
            },
            "description": "Requires the `admin` scope. Returns rolling counters for identify/nearby/resolve calls and OpenRouter cost telemetry.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        },
        {
          "name": "Admin: list users",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/v1/admin/users",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "v1",
                "admin",
                "users"
              ]
            },
            "description": "Requires the `admin` scope. Returns registered users.",
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{sessionToken}}",
                  "type": "string"
                }
              ]
            }
          }
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3001",
      "type": "string"
    },
    {
      "key": "sessionToken",
      "value": "",
      "type": "string"
    }
  ]
}
