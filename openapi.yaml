# GENERATED FILE — do not edit by hand.
# Source: packages/core/src/schemas/*.ts
# Regenerate: bun run openapi
openapi: 3.1.0
info:
  title: Mapvest API
  version: 0.1.0-alpha.0
  description: Mapvest turns places and objects into investable tickers. All request and response shapes are generated from zod schemas in `packages/core/src/schemas` — never hand-edit `openapi.yaml`.
  contact:
    name: Mapvest
    url: https://github.com/jawauntb/mapvest
  license:
    name: UNLICENSED
servers:
  - url: http://localhost:3001
    description: Local dev
  - url: https://mapvest-api.up.railway.app
    description: Railway (staging/production)
tags:
  - name: system
    description: Health + system endpoints
  - name: identify
    description: Photo → investable identification
  - name: nearby
    description: Location-driven place lookup
  - name: widget
    description: Home-screen widget data (iOS WidgetKit / Android App Widget)
  - name: finance
    description: Ticker / comparable / ETF resolution
  - name: auth
    description: Passwordless email sign-in
  - name: billing
    description: Entitlements + $19.99/mo subscription checkout
  - name: admin
    description: Requires the `admin` scope
security:
  - bearerAuth: []
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: "Session token issued by `POST /v1/auth/session`. Attach as `Authorization: Bearer <token>`."
  schemas:
    Confidence:
      type: string
      enum:
        - high
        - medium
        - low
    Source:
      type: object
      properties:
        provider:
          type: string
          enum:
            - exa
            - openrouter
            - gemini
            - massive
            - yahoo
            - polygon
            - sec
            - fred
            - manual
        url:
          type: string
          format: uri
        fetchedAt:
          type: string
        confidence:
          type: string
          enum:
            - high
            - medium
            - low
      required:
        - provider
        - fetchedAt
        - confidence
    LatLng:
      type: object
      properties:
        lat:
          type: number
          minimum: -90
          maximum: 90
        lng:
          type: number
          minimum: -180
          maximum: 180
      required:
        - lat
        - lng
    Ticker:
      type: object
      properties:
        symbol:
          type: string
        exchange:
          type: string
        parent:
          type: string
      required:
        - symbol
    Brand:
      type: object
      properties:
        name:
          type: string
        parent:
          type: string
        isPublic:
          type: boolean
        ticker:
          type: object
          properties:
            symbol:
              type: string
            exchange:
              type: string
            parent:
              type: string
          required:
            - symbol
        sector:
          type: string
        logo:
          type: string
          format: uri
      required:
        - name
        - isPublic
    EtfExposure:
      type: object
      properties:
        ticker:
          type: string
        name:
          type: string
        weight:
          type: number
          minimum: 0
          maximum: 1
        source:
          type: object
          properties:
            provider:
              type: string
              enum:
                - exa
                - openrouter
                - gemini
                - massive
                - yahoo
                - polygon
                - sec
                - fred
                - manual
            url:
              type: string
              format: uri
            fetchedAt:
              type: string
            confidence:
              type: string
              enum:
                - high
                - medium
                - low
          required:
            - provider
            - fetchedAt
            - confidence
      required:
        - ticker
        - name
        - weight
        - source
    Comparable:
      type: object
      properties:
        ticker:
          type: string
        name:
          type: string
        score:
          type: number
          minimum: 0
          maximum: 1
        reasoning:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - ticker
        - name
        - score
        - reasoning
        - sources
    Investable:
      type: object
      properties:
        brand:
          type: object
          properties:
            name:
              type: string
            parent:
              type: string
            isPublic:
              type: boolean
            ticker:
              type: object
              properties:
                symbol:
                  type: string
                exchange:
                  type: string
                parent:
                  type: string
              required:
                - symbol
            sector:
              type: string
            logo:
              type: string
              format: uri
          required:
            - name
            - isPublic
        comparables:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              name:
                type: string
              score:
                type: number
                minimum: 0
                maximum: 1
              reasoning:
                type: string
              sources:
                type: array
                items:
                  type: object
                  properties:
                    provider:
                      type: string
                      enum:
                        - exa
                        - openrouter
                        - gemini
                        - massive
                        - yahoo
                        - polygon
                        - sec
                        - fred
                        - manual
                    url:
                      type: string
                      format: uri
                    fetchedAt:
                      type: string
                    confidence:
                      type: string
                      enum:
                        - high
                        - medium
                        - low
                  required:
                    - provider
                    - fetchedAt
                    - confidence
            required:
              - ticker
              - name
              - score
              - reasoning
              - sources
          default: []
        etfs:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              name:
                type: string
              weight:
                type: number
                minimum: 0
                maximum: 1
              source:
                type: object
                properties:
                  provider:
                    type: string
                    enum:
                      - exa
                      - openrouter
                      - gemini
                      - massive
                      - yahoo
                      - polygon
                      - sec
                      - fred
                      - manual
                  url:
                    type: string
                    format: uri
                  fetchedAt:
                    type: string
                  confidence:
                    type: string
                    enum:
                      - high
                      - medium
                      - low
                required:
                  - provider
                  - fetchedAt
                  - confidence
            required:
              - ticker
              - name
              - weight
              - source
          default: []
        confidence:
          type: string
          enum:
            - high
            - medium
            - low
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
        quote:
          type: object
          properties:
            symbol:
              type: string
            price:
              type: number
            change:
              type: number
            changePct:
              type: number
            currency:
              type: string
            ts:
              type: string
            disclaimer:
              type: string
            name:
              type: string
            provider:
              type: string
              enum:
                - massive
                - yahoo
            freshness:
              type: string
              enum:
                - real-time
                - delayed
                - end-of-day
                - unknown
          required:
            - symbol
            - price
            - change
            - changePct
            - currency
            - ts
            - disclaimer
      required:
        - brand
        - confidence
        - sources
    PhotoIdentification:
      type: object
      properties:
        visibleText:
          type: array
          items:
            type: string
          default: []
        detected:
          type: array
          items:
            type: object
            properties:
              brand:
                type: string
              product:
                type: string
              sector:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - confidence
        location:
          type: object
          properties:
            lat:
              type: number
              minimum: -90
              maximum: 90
            lng:
              type: number
              minimum: -180
              maximum: 180
          required:
            - lat
            - lng
        modelUsed:
          type: string
      required:
        - detected
        - modelUsed
    IdentifyRequest:
      type: object
      properties:
        location:
          type: object
          properties:
            lat:
              type: number
              minimum: -90
              maximum: 90
            lng:
              type: number
              minimum: -180
              maximum: 180
          required:
            - lat
            - lng
    IdentifyResponse:
      type: object
      properties:
        identification:
          type: object
          properties:
            visibleText:
              type: array
              items:
                type: string
              default: []
            detected:
              type: array
              items:
                type: object
                properties:
                  brand:
                    type: string
                  product:
                    type: string
                  sector:
                    type: string
                  confidence:
                    type: string
                    enum:
                      - high
                      - medium
                      - low
                required:
                  - confidence
            location:
              type: object
              properties:
                lat:
                  type: number
                  minimum: -90
                  maximum: 90
                lng:
                  type: number
                  minimum: -180
                  maximum: 180
              required:
                - lat
                - lng
            modelUsed:
              type: string
          required:
            - detected
            - modelUsed
        investables:
          type: array
          items:
            type: object
            properties:
              brand:
                type: object
                properties:
                  name:
                    type: string
                  parent:
                    type: string
                  isPublic:
                    type: boolean
                  ticker:
                    type: object
                    properties:
                      symbol:
                        type: string
                      exchange:
                        type: string
                      parent:
                        type: string
                    required:
                      - symbol
                  sector:
                    type: string
                  logo:
                    type: string
                    format: uri
                required:
                  - name
                  - isPublic
              comparables:
                type: array
                items:
                  type: object
                  properties:
                    ticker:
                      type: string
                    name:
                      type: string
                    score:
                      type: number
                      minimum: 0
                      maximum: 1
                    reasoning:
                      type: string
                    sources:
                      type: array
                      items:
                        type: object
                        properties:
                          provider:
                            type: string
                            enum:
                              - exa
                              - openrouter
                              - gemini
                              - massive
                              - yahoo
                              - polygon
                              - sec
                              - fred
                              - manual
                          url:
                            type: string
                            format: uri
                          fetchedAt:
                            type: string
                          confidence:
                            type: string
                            enum:
                              - high
                              - medium
                              - low
                        required:
                          - provider
                          - fetchedAt
                          - confidence
                  required:
                    - ticker
                    - name
                    - score
                    - reasoning
                    - sources
                default: []
              etfs:
                type: array
                items:
                  type: object
                  properties:
                    ticker:
                      type: string
                    name:
                      type: string
                    weight:
                      type: number
                      minimum: 0
                      maximum: 1
                    source:
                      type: object
                      properties:
                        provider:
                          type: string
                          enum:
                            - exa
                            - openrouter
                            - gemini
                            - massive
                            - yahoo
                            - polygon
                            - sec
                            - fred
                            - manual
                        url:
                          type: string
                          format: uri
                        fetchedAt:
                          type: string
                        confidence:
                          type: string
                          enum:
                            - high
                            - medium
                            - low
                      required:
                        - provider
                        - fetchedAt
                        - confidence
                  required:
                    - ticker
                    - name
                    - weight
                    - source
                default: []
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
              sources:
                type: array
                items:
                  type: object
                  properties:
                    provider:
                      type: string
                      enum:
                        - exa
                        - openrouter
                        - gemini
                        - massive
                        - yahoo
                        - polygon
                        - sec
                        - fred
                        - manual
                    url:
                      type: string
                      format: uri
                    fetchedAt:
                      type: string
                    confidence:
                      type: string
                      enum:
                        - high
                        - medium
                        - low
                  required:
                    - provider
                    - fetchedAt
                    - confidence
              quote:
                type: object
                properties:
                  symbol:
                    type: string
                  price:
                    type: number
                  change:
                    type: number
                  changePct:
                    type: number
                  currency:
                    type: string
                  ts:
                    type: string
                  disclaimer:
                    type: string
                  name:
                    type: string
                  provider:
                    type: string
                    enum:
                      - massive
                      - yahoo
                  freshness:
                    type: string
                    enum:
                      - real-time
                      - delayed
                      - end-of-day
                      - unknown
                required:
                  - symbol
                  - price
                  - change
                  - changePct
                  - currency
                  - ts
                  - disclaimer
            required:
              - brand
              - confidence
              - sources
      required:
        - identification
        - investables
    Find:
      type: object
      properties:
        id:
          type: string
        brand:
          type: string
        ticker:
          type: string
        isPublic:
          type: boolean
        comparable:
          type: string
        confidence:
          type: string
          enum:
            - high
            - medium
            - low
        lat:
          type: number
        lng:
          type: number
        foundPrice:
          type: number
        createdAt:
          type: string
      required:
        - id
        - brand
        - confidence
        - createdAt
    FindsResponse:
      type: object
      properties:
        finds:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              brand:
                type: string
              ticker:
                type: string
              isPublic:
                type: boolean
              comparable:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
              lat:
                type: number
              lng:
                type: number
              foundPrice:
                type: number
              createdAt:
                type: string
            required:
              - id
              - brand
              - confidence
              - createdAt
        count:
          type: number
      required:
        - finds
        - count
    NearbyRequest:
      type: object
      properties:
        lat:
          type: number
        lng:
          type: number
        radius:
          type: number
          default: 500
        limit:
          type: number
          default: 25
      required:
        - lat
        - lng
    NearbyItem:
      type: object
      properties:
        place:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            location:
              type: object
              properties:
                lat:
                  type: number
                  minimum: -90
                  maximum: 90
                lng:
                  type: number
                  minimum: -180
                  maximum: 180
              required:
                - lat
                - lng
            types:
              type: array
              items:
                type: string
              default: []
          required:
            - id
            - name
            - location
        investable:
          type: object
          properties:
            brand:
              type: object
              properties:
                name:
                  type: string
                parent:
                  type: string
                isPublic:
                  type: boolean
                ticker:
                  type: object
                  properties:
                    symbol:
                      type: string
                    exchange:
                      type: string
                    parent:
                      type: string
                  required:
                    - symbol
                sector:
                  type: string
                logo:
                  type: string
                  format: uri
              required:
                - name
                - isPublic
            comparables:
              type: array
              items:
                type: object
                properties:
                  ticker:
                    type: string
                  name:
                    type: string
                  score:
                    type: number
                    minimum: 0
                    maximum: 1
                  reasoning:
                    type: string
                  sources:
                    type: array
                    items:
                      type: object
                      properties:
                        provider:
                          type: string
                          enum:
                            - exa
                            - openrouter
                            - gemini
                            - massive
                            - yahoo
                            - polygon
                            - sec
                            - fred
                            - manual
                        url:
                          type: string
                          format: uri
                        fetchedAt:
                          type: string
                        confidence:
                          type: string
                          enum:
                            - high
                            - medium
                            - low
                      required:
                        - provider
                        - fetchedAt
                        - confidence
                required:
                  - ticker
                  - name
                  - score
                  - reasoning
                  - sources
              default: []
            etfs:
              type: array
              items:
                type: object
                properties:
                  ticker:
                    type: string
                  name:
                    type: string
                  weight:
                    type: number
                    minimum: 0
                    maximum: 1
                  source:
                    type: object
                    properties:
                      provider:
                        type: string
                        enum:
                          - exa
                          - openrouter
                          - gemini
                          - massive
                          - yahoo
                          - polygon
                          - sec
                          - fred
                          - manual
                      url:
                        type: string
                        format: uri
                      fetchedAt:
                        type: string
                      confidence:
                        type: string
                        enum:
                          - high
                          - medium
                          - low
                    required:
                      - provider
                      - fetchedAt
                      - confidence
                required:
                  - ticker
                  - name
                  - weight
                  - source
              default: []
            confidence:
              type: string
              enum:
                - high
                - medium
                - low
            sources:
              type: array
              items:
                type: object
                properties:
                  provider:
                    type: string
                    enum:
                      - exa
                      - openrouter
                      - gemini
                      - massive
                      - yahoo
                      - polygon
                      - sec
                      - fred
                      - manual
                  url:
                    type: string
                    format: uri
                  fetchedAt:
                    type: string
                  confidence:
                    type: string
                    enum:
                      - high
                      - medium
                      - low
                required:
                  - provider
                  - fetchedAt
                  - confidence
            quote:
              type: object
              properties:
                symbol:
                  type: string
                price:
                  type: number
                change:
                  type: number
                changePct:
                  type: number
                currency:
                  type: string
                ts:
                  type: string
                disclaimer:
                  type: string
                name:
                  type: string
                provider:
                  type: string
                  enum:
                    - massive
                    - yahoo
                freshness:
                  type: string
                  enum:
                    - real-time
                    - delayed
                    - end-of-day
                    - unknown
              required:
                - symbol
                - price
                - change
                - changePct
                - currency
                - ts
                - disclaimer
          required:
            - brand
            - confidence
            - sources
      required:
        - place
    NearbyResponse:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              place:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  location:
                    type: object
                    properties:
                      lat:
                        type: number
                        minimum: -90
                        maximum: 90
                      lng:
                        type: number
                        minimum: -180
                        maximum: 180
                    required:
                      - lat
                      - lng
                  types:
                    type: array
                    items:
                      type: string
                    default: []
                required:
                  - id
                  - name
                  - location
              investable:
                type: object
                properties:
                  brand:
                    type: object
                    properties:
                      name:
                        type: string
                      parent:
                        type: string
                      isPublic:
                        type: boolean
                      ticker:
                        type: object
                        properties:
                          symbol:
                            type: string
                          exchange:
                            type: string
                          parent:
                            type: string
                        required:
                          - symbol
                      sector:
                        type: string
                      logo:
                        type: string
                        format: uri
                    required:
                      - name
                      - isPublic
                  comparables:
                    type: array
                    items:
                      type: object
                      properties:
                        ticker:
                          type: string
                        name:
                          type: string
                        score:
                          type: number
                          minimum: 0
                          maximum: 1
                        reasoning:
                          type: string
                        sources:
                          type: array
                          items:
                            type: object
                            properties:
                              provider:
                                type: string
                                enum:
                                  - exa
                                  - openrouter
                                  - gemini
                                  - massive
                                  - yahoo
                                  - polygon
                                  - sec
                                  - fred
                                  - manual
                              url:
                                type: string
                                format: uri
                              fetchedAt:
                                type: string
                              confidence:
                                type: string
                                enum:
                                  - high
                                  - medium
                                  - low
                            required:
                              - provider
                              - fetchedAt
                              - confidence
                      required:
                        - ticker
                        - name
                        - score
                        - reasoning
                        - sources
                    default: []
                  etfs:
                    type: array
                    items:
                      type: object
                      properties:
                        ticker:
                          type: string
                        name:
                          type: string
                        weight:
                          type: number
                          minimum: 0
                          maximum: 1
                        source:
                          type: object
                          properties:
                            provider:
                              type: string
                              enum:
                                - exa
                                - openrouter
                                - gemini
                                - massive
                                - yahoo
                                - polygon
                                - sec
                                - fred
                                - manual
                            url:
                              type: string
                              format: uri
                            fetchedAt:
                              type: string
                            confidence:
                              type: string
                              enum:
                                - high
                                - medium
                                - low
                          required:
                            - provider
                            - fetchedAt
                            - confidence
                      required:
                        - ticker
                        - name
                        - weight
                        - source
                    default: []
                  confidence:
                    type: string
                    enum:
                      - high
                      - medium
                      - low
                  sources:
                    type: array
                    items:
                      type: object
                      properties:
                        provider:
                          type: string
                          enum:
                            - exa
                            - openrouter
                            - gemini
                            - massive
                            - yahoo
                            - polygon
                            - sec
                            - fred
                            - manual
                        url:
                          type: string
                          format: uri
                        fetchedAt:
                          type: string
                        confidence:
                          type: string
                          enum:
                            - high
                            - medium
                            - low
                      required:
                        - provider
                        - fetchedAt
                        - confidence
                  quote:
                    type: object
                    properties:
                      symbol:
                        type: string
                      price:
                        type: number
                      change:
                        type: number
                      changePct:
                        type: number
                      currency:
                        type: string
                      ts:
                        type: string
                      disclaimer:
                        type: string
                      name:
                        type: string
                      provider:
                        type: string
                        enum:
                          - massive
                          - yahoo
                      freshness:
                        type: string
                        enum:
                          - real-time
                          - delayed
                          - end-of-day
                          - unknown
                    required:
                      - symbol
                      - price
                      - change
                      - changePct
                      - currency
                      - ts
                      - disclaimer
                required:
                  - brand
                  - confidence
                  - sources
            required:
              - place
      required:
        - items
    WidgetNearbyItem:
      type: object
      properties:
        name:
          type: string
        ticker:
          type: string
        isPublic:
          type: boolean
        sector:
          type: string
        distanceM:
          type: number
        price:
          type: number
        changePct:
          type: number
        location:
          type: object
          properties:
            lat:
              type: number
              minimum: -90
              maximum: 90
            lng:
              type: number
              minimum: -180
              maximum: 180
          required:
            - lat
            - lng
      required:
        - name
        - location
    WidgetNearbyResponse:
      type: object
      properties:
        origin:
          type: object
          properties:
            lat:
              type: number
              minimum: -90
              maximum: 90
            lng:
              type: number
              minimum: -180
              maximum: 180
          required:
            - lat
            - lng
        items:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              ticker:
                type: string
              isPublic:
                type: boolean
              sector:
                type: string
              distanceM:
                type: number
              price:
                type: number
              changePct:
                type: number
              location:
                type: object
                properties:
                  lat:
                    type: number
                    minimum: -90
                    maximum: 90
                  lng:
                    type: number
                    minimum: -180
                    maximum: 180
                required:
                  - lat
                  - lng
            required:
              - name
              - location
        mapSnapshotUrl:
          type: string
          format: uri
        generatedAt:
          type: string
      required:
        - origin
        - items
        - generatedAt
    ResolveComparableRequest:
      type: object
      properties:
        brand:
          type: string
        hintSector:
          type: string
      required:
        - brand
    ResolveComparableResponse:
      type: object
      properties:
        brand:
          type: object
          properties:
            name:
              type: string
            parent:
              type: string
            isPublic:
              type: boolean
            ticker:
              type: object
              properties:
                symbol:
                  type: string
                exchange:
                  type: string
                parent:
                  type: string
              required:
                - symbol
            sector:
              type: string
            logo:
              type: string
              format: uri
          required:
            - name
            - isPublic
        comparables:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              name:
                type: string
              score:
                type: number
                minimum: 0
                maximum: 1
              reasoning:
                type: string
              sources:
                type: array
                items:
                  type: object
                  properties:
                    provider:
                      type: string
                      enum:
                        - exa
                        - openrouter
                        - gemini
                        - massive
                        - yahoo
                        - polygon
                        - sec
                        - fred
                        - manual
                    url:
                      type: string
                      format: uri
                    fetchedAt:
                      type: string
                    confidence:
                      type: string
                      enum:
                        - high
                        - medium
                        - low
                  required:
                    - provider
                    - fetchedAt
                    - confidence
            required:
              - ticker
              - name
              - score
              - reasoning
              - sources
        etfs:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              name:
                type: string
              weight:
                type: number
                minimum: 0
                maximum: 1
              source:
                type: object
                properties:
                  provider:
                    type: string
                    enum:
                      - exa
                      - openrouter
                      - gemini
                      - massive
                      - yahoo
                      - polygon
                      - sec
                      - fred
                      - manual
                  url:
                    type: string
                    format: uri
                  fetchedAt:
                    type: string
                  confidence:
                    type: string
                    enum:
                      - high
                      - medium
                      - low
                required:
                  - provider
                  - fetchedAt
                  - confidence
            required:
              - ticker
              - name
              - weight
              - source
      required:
        - brand
        - comparables
        - etfs
    ChartResponse:
      type: object
      properties:
        ticker:
          type: string
        type:
          type: string
        period:
          type: string
        image:
          type: object
          properties:
            mime:
              type: string
            data:
              type: string
            filename:
              type: string
          required:
            - mime
            - data
        levels:
          type: object
          properties:
            poc:
              type: number
            vah:
              type: number
            val:
              type: number
        meta:
          type: object
          additionalProperties: {}
        provider:
          type: string
        providerNote:
          type: string
        sourceUrl:
          type: string
          format: uri
      required:
        - ticker
        - period
        - image
    QuoteHistoryPoint:
      type: object
      properties:
        ts:
          type: number
        close:
          type: number
      required:
        - ts
        - close
    QuoteHistoryResponse:
      type: object
      properties:
        ticker:
          type: string
        period:
          type: string
          enum:
            - 5d
            - 1mo
            - 3mo
            - 6mo
            - 1y
            - 2y
        interval:
          type: string
          enum:
            - 15m
            - 1d
            - 1w
          default: 1d
        points:
          type: array
          items:
            type: object
            properties:
              ts:
                type: number
              close:
                type: number
            required:
              - ts
              - close
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - ticker
        - period
        - points
        - sources
    AggregatePoint:
      type: object
      properties:
        ts:
          type: number
        open:
          type: number
        high:
          type: number
        low:
          type: number
        close:
          type: number
        volume:
          type: number
        vwap:
          type: number
        transactions:
          type: number
      required:
        - ts
        - open
        - high
        - low
        - close
    AggregatesResponse:
      type: object
      properties:
        symbol:
          type: string
        from:
          type: string
        to:
          type: string
        multiplier:
          type: number
        timespan:
          type: string
        points:
          type: array
          items:
            type: object
            properties:
              ts:
                type: number
              open:
                type: number
              high:
                type: number
              low:
                type: number
              close:
                type: number
              volume:
                type: number
              vwap:
                type: number
              transactions:
                type: number
            required:
              - ts
              - open
              - high
              - low
              - close
        nextCursor:
          type: string
        requestId:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - symbol
        - from
        - to
        - multiplier
        - timespan
        - points
        - sources
    OptionContract:
      type: object
      properties:
        ticker:
          type: string
        underlyingTicker:
          type: string
        contractType:
          type: string
          enum:
            - call
            - put
            - other
        expirationDate:
          type: string
        strikePrice:
          type: number
        exerciseStyle:
          type: string
          enum:
            - american
            - bermudan
            - european
        sharesPerContract:
          type: number
        primaryExchange:
          type: string
        cfi:
          type: string
      required:
        - ticker
    OptionSnapshot:
      type: object
      properties:
        ticker:
          type: string
        underlyingTicker:
          type: string
        contractType:
          type: string
          enum:
            - call
            - put
            - other
        expirationDate:
          type: string
        strikePrice:
          type: number
        exerciseStyle:
          type: string
          enum:
            - american
            - bermudan
            - european
        sharesPerContract:
          type: number
        primaryExchange:
          type: string
        cfi:
          type: string
        breakEvenPrice:
          type: number
        impliedVolatility:
          type: number
        openInterest:
          type: number
        greeks:
          type: object
          properties:
            delta:
              type: number
            gamma:
              type: number
            theta:
              type: number
            vega:
              type: number
        quote:
          type: object
          properties:
            bid:
              type: number
            ask:
              type: number
            bidSize:
              type: number
            askSize:
              type: number
            ts:
              type: number
        trade:
          type: object
          properties:
            price:
              type: number
            size:
              type: number
            ts:
              type: number
        day:
          type: object
          properties:
            open:
              type: number
            high:
              type: number
            low:
              type: number
            close:
              type: number
            volume:
              type: number
      required:
        - ticker
    OptionsResponse:
      type: object
      properties:
        underlyingTicker:
          type: string
        contracts:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              underlyingTicker:
                type: string
              contractType:
                type: string
                enum:
                  - call
                  - put
                  - other
              expirationDate:
                type: string
              strikePrice:
                type: number
              exerciseStyle:
                type: string
                enum:
                  - american
                  - bermudan
                  - european
              sharesPerContract:
                type: number
              primaryExchange:
                type: string
              cfi:
                type: string
              breakEvenPrice:
                type: number
              impliedVolatility:
                type: number
              openInterest:
                type: number
              greeks:
                type: object
                properties:
                  delta:
                    type: number
                  gamma:
                    type: number
                  theta:
                    type: number
                  vega:
                    type: number
              quote:
                type: object
                properties:
                  bid:
                    type: number
                  ask:
                    type: number
                  bidSize:
                    type: number
                  askSize:
                    type: number
                  ts:
                    type: number
              trade:
                type: object
                properties:
                  price:
                    type: number
                  size:
                    type: number
                  ts:
                    type: number
              day:
                type: object
                properties:
                  open:
                    type: number
                  high:
                    type: number
                  low:
                    type: number
                  close:
                    type: number
                  volume:
                    type: number
            required:
              - ticker
        nextCursor:
          type: string
        requestId:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - underlyingTicker
        - contracts
        - sources
    OptionContractsResponse:
      type: object
      properties:
        contracts:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              underlyingTicker:
                type: string
              contractType:
                type: string
                enum:
                  - call
                  - put
                  - other
              expirationDate:
                type: string
              strikePrice:
                type: number
              exerciseStyle:
                type: string
                enum:
                  - american
                  - bermudan
                  - european
              sharesPerContract:
                type: number
              primaryExchange:
                type: string
              cfi:
                type: string
            required:
              - ticker
        nextCursor:
          type: string
        requestId:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - contracts
        - sources
    FinancialRatiosRequest:
      type: object
      properties:
        ticker:
          type: string
          pattern: ^[A-Z][A-Z0-9._-]{0,14}$
        limit:
          type: integer
          minimum: 1
          maximum: 500
          default: 1
        cursor:
          type: string
          minLength: 1
          maxLength: 2048
          pattern: ^[^\s]+$
      required:
        - ticker
    FinancialRatio:
      type: object
      properties:
        ticker:
          type: string
        date:
          type: string
        averageVolume:
          type: number
        cash:
          type: number
        cik:
          type: string
        current:
          type: number
        debtToEquity:
          type: number
        dividendYield:
          type: number
        earningsPerShare:
          type: number
        enterpriseValue:
          type: number
        evToEbitda:
          type: number
        evToSales:
          type: number
        freeCashFlow:
          type: number
        marketCap:
          type: number
        price:
          type: number
        priceToBook:
          type: number
        priceToCashFlow:
          type: number
        priceToEarnings:
          type: number
        priceToFreeCashFlow:
          type: number
        priceToSales:
          type: number
        quick:
          type: number
        returnOnAssets:
          type: number
        returnOnEquity:
          type: number
      required:
        - ticker
    FinancialRatiosResponse:
      type: object
      properties:
        ticker:
          type: string
        ratios:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              date:
                type: string
              averageVolume:
                type: number
              cash:
                type: number
              cik:
                type: string
              current:
                type: number
              debtToEquity:
                type: number
              dividendYield:
                type: number
              earningsPerShare:
                type: number
              enterpriseValue:
                type: number
              evToEbitda:
                type: number
              evToSales:
                type: number
              freeCashFlow:
                type: number
              marketCap:
                type: number
              price:
                type: number
              priceToBook:
                type: number
              priceToCashFlow:
                type: number
              priceToEarnings:
                type: number
              priceToFreeCashFlow:
                type: number
              priceToSales:
                type: number
              quick:
                type: number
              returnOnAssets:
                type: number
              returnOnEquity:
                type: number
            required:
              - ticker
        nextCursor:
          type: string
        requestId:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - ticker
        - ratios
        - sources
    OptionSummaryRequest:
      type: object
      properties:
        underlying:
          type: string
          pattern: ^[A-Z][A-Z0-9._-]{0,14}$
        contract:
          type: string
          pattern: ^O:[A-Z0-9._-]{1,48}$
      required:
        - underlying
        - contract
    OptionSummary:
      type: object
      properties:
        ticker:
          type: string
        underlyingTicker:
          type: string
        contractType:
          type: string
          enum:
            - call
            - put
            - other
        expirationDate:
          type: string
        strikePrice:
          type: number
        exerciseStyle:
          type: string
          enum:
            - american
            - bermudan
            - european
        sharesPerContract:
          type: number
        primaryExchange:
          type: string
        cfi:
          type: string
        breakEvenPrice:
          type: number
        impliedVolatility:
          type: number
        openInterest:
          type: number
        greeks:
          type: object
          properties:
            delta:
              type: number
            gamma:
              type: number
            theta:
              type: number
            vega:
              type: number
        quote:
          type: object
          properties:
            bid:
              type: number
            ask:
              type: number
            bidSize:
              type: number
            askSize:
              type: number
            ts:
              type: number
        trade:
          type: object
          properties:
            price:
              type: number
            size:
              type: number
            ts:
              type: number
        day:
          type: object
          properties:
            open:
              type: number
            high:
              type: number
            low:
              type: number
            close:
              type: number
            volume:
              type: number
        underlyingPrice:
          type: number
        change:
          type: number
        changePct:
          type: number
        fmv:
          type: number
        fmvLastUpdated:
          type: number
      required:
        - ticker
    OptionSummaryResponse:
      type: object
      properties:
        underlyingTicker:
          type: string
        contractTicker:
          type: string
        summary:
          type: object
          properties:
            ticker:
              type: string
            underlyingTicker:
              type: string
            contractType:
              type: string
              enum:
                - call
                - put
                - other
            expirationDate:
              type: string
            strikePrice:
              type: number
            exerciseStyle:
              type: string
              enum:
                - american
                - bermudan
                - european
            sharesPerContract:
              type: number
            primaryExchange:
              type: string
            cfi:
              type: string
            breakEvenPrice:
              type: number
            impliedVolatility:
              type: number
            openInterest:
              type: number
            greeks:
              type: object
              properties:
                delta:
                  type: number
                gamma:
                  type: number
                theta:
                  type: number
                vega:
                  type: number
            quote:
              type: object
              properties:
                bid:
                  type: number
                ask:
                  type: number
                bidSize:
                  type: number
                askSize:
                  type: number
                ts:
                  type: number
            trade:
              type: object
              properties:
                price:
                  type: number
                size:
                  type: number
                ts:
                  type: number
            day:
              type: object
              properties:
                open:
                  type: number
                high:
                  type: number
                low:
                  type: number
                close:
                  type: number
                volume:
                  type: number
            underlyingPrice:
              type: number
            change:
              type: number
            changePct:
              type: number
            fmv:
              type: number
            fmvLastUpdated:
              type: number
          required:
            - ticker
        requestId:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - underlyingTicker
        - contractTicker
        - summary
        - sources
    OptionBarsRequest:
      type: object
      properties:
        ticker:
          type: string
          pattern: ^O:[A-Z0-9._-]{1,48}$
        from:
          type: string
          pattern: ^\d{4}-\d{2}-\d{2}$
        to:
          type: string
          pattern: ^\d{4}-\d{2}-\d{2}$
        multiplier:
          type: integer
          minimum: 1
          maximum: 1000
          default: 1
        timespan:
          type: string
          enum:
            - minute
            - hour
            - day
            - week
            - month
            - quarter
            - year
          default: day
        adjusted:
          type: boolean
          default: true
        cursor:
          type: string
          minLength: 1
          maxLength: 2048
          pattern: ^[^\s]+$
      required:
        - ticker
        - from
        - to
    OptionBarsResponse:
      type: object
      properties:
        contractTicker:
          type: string
        from:
          type: string
        to:
          type: string
        multiplier:
          type: number
        timespan:
          type: string
        points:
          type: array
          items:
            type: object
            properties:
              ts:
                type: number
              open:
                type: number
              high:
                type: number
              low:
                type: number
              close:
                type: number
              volume:
                type: number
              vwap:
                type: number
              transactions:
                type: number
            required:
              - ts
              - open
              - high
              - low
              - close
        nextCursor:
          type: string
        requestId:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - contractTicker
        - from
        - to
        - multiplier
        - timespan
        - points
        - sources
    CorporateEvent:
      type: object
      properties:
        id:
          type: string
        ticker:
          type: string
        type:
          type: string
        date:
          type: string
        status:
          type: string
        description:
          type: string
        sourceUrl:
          type: string
          format: uri
        provider:
          type: string
          enum:
            - massive
            - tmx
        companyName:
          type: string
        isin:
          type: string
        tradingVenue:
          type: string
        tmxRecordId:
          type: string
      required:
        - ticker
        - type
    MarketEventsResponse:
      type: object
      properties:
        ticker:
          type: string
        events:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              ticker:
                type: string
              type:
                type: string
              date:
                type: string
              status:
                type: string
              description:
                type: string
              sourceUrl:
                type: string
                format: uri
              provider:
                type: string
                enum:
                  - massive
                  - tmx
              companyName:
                type: string
              isin:
                type: string
              tradingVenue:
                type: string
              tmxRecordId:
                type: string
            required:
              - ticker
              - type
        tmxAvailable:
          type: boolean
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - events
        - sources
    MarketDataCapabilities:
      type: object
      properties:
        provider:
          type: string
          enum:
            - massive
            - yahoo
        configured:
          type: boolean
        freshness:
          type: string
          enum:
            - real-time
            - delayed
            - end-of-day
            - unknown
        datasets:
          type: object
          additionalProperties:
            type: object
            properties:
              supported:
                type: boolean
              access:
                type: string
                enum:
                  - primary
                  - fallback
                  - unconfigured
              note:
                type: string
            required:
              - supported
              - access
        subscription:
          type: object
          properties:
            stocks:
              type: string
            options:
              type: string
            events:
              type: string
      required:
        - provider
        - configured
        - freshness
        - datasets
        - subscription
    AnalysisSnapshot:
      type: object
      properties:
        ticker:
          type: string
        name:
          type: string
        sector:
          type: string
        industry:
          type: string
        price:
          type: number
        change:
          type: number
        changePercent:
          type: number
        marketCap:
          anyOf:
            - type: string
            - type: number
        trailingPe:
          anyOf:
            - type: string
            - type: number
        annualVolatility:
          type: number
        fiftyTwoWeekHigh:
          type: number
        fiftyTwoWeekLow:
          type: number
        brief:
          type: string
        briefProvider:
          type: string
        sourceUrl:
          type: string
          format: uri
      required:
        - ticker
    CockpitResponse:
      type: object
      properties:
        rows:
          type: array
          items:
            type: object
            properties:
              rank:
                type: number
              ticker:
                type: string
              score:
                type: number
              lane:
                type: string
              ridge:
                anyOf:
                  - type: string
                  - type: number
              flow:
                anyOf:
                  - type: string
                  - type: number
              auction:
                anyOf:
                  - type: string
                  - type: number
            required:
              - ticker
        tickers:
          type: array
          items:
            type: string
        meta:
          type: object
          additionalProperties: {}
        sourceUrl:
          type: string
          format: uri
      required:
        - rows
        - tickers
    AlertsResponse:
      type: object
      properties:
        alerts:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              title:
                type: string
              severity:
                anyOf:
                  - type: string
                  - type: number
              summary:
                type: string
              message:
                type: string
        tickers:
          type: array
          items:
            type: string
        meta:
          type: object
          additionalProperties: {}
        sourceUrl:
          type: string
          format: uri
      required:
        - alerts
        - tickers
    AgentChatRequest:
      type: object
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 4000
        ticker:
          type: string
        threadId:
          type: string
      required:
        - message
    AgentChatResponse:
      type: object
      properties:
        threadId:
          type: string
        ticker:
          type: string
        article:
          type: object
          properties:
            id:
              type: string
            role:
              type: string
              enum:
                - user
                - assistant
            content:
              type: string
            createdAt:
              type: string
            interesting:
              type: array
              items:
                type: string
              default: []
            ideas:
              type: array
              items:
                type: object
                properties:
                  title:
                    type: string
                  thesis:
                    type: string
                  disposition:
                    type: string
                  findings:
                    type: array
                    items:
                      type: string
                    default: []
                required:
                  - title
                  - thesis
              default: []
            toolsUsed:
              type: array
              items:
                type: string
              default: []
            sources:
              type: array
              items:
                type: object
                properties:
                  label:
                    type: string
                  url:
                    type: string
                    format: uri
                required:
                  - label
              default: []
            chartTickers:
              type: array
              items:
                type: string
              default: []
            mode:
              type: string
            error:
              type: string
          required:
            - id
            - role
            - content
            - createdAt
        userMessage:
          type: object
          properties:
            id:
              type: string
            role:
              type: string
              enum:
                - user
                - assistant
            content:
              type: string
            createdAt:
              type: string
            interesting:
              type: array
              items:
                type: string
              default: []
            ideas:
              type: array
              items:
                type: object
                properties:
                  title:
                    type: string
                  thesis:
                    type: string
                  disposition:
                    type: string
                  findings:
                    type: array
                    items:
                      type: string
                    default: []
                required:
                  - title
                  - thesis
              default: []
            toolsUsed:
              type: array
              items:
                type: string
              default: []
            sources:
              type: array
              items:
                type: object
                properties:
                  label:
                    type: string
                  url:
                    type: string
                    format: uri
                required:
                  - label
              default: []
            chartTickers:
              type: array
              items:
                type: string
              default: []
            mode:
              type: string
            error:
              type: string
          required:
            - id
            - role
            - content
            - createdAt
        safety:
          type: object
          properties:
            liveTradingForbidden:
              type: boolean
            orderSubmissionAllowed:
              type: boolean
          required:
            - liveTradingForbidden
            - orderSubmissionAllowed
        sourceUrl:
          type: string
          format: uri
        provider:
          type: string
      required:
        - article
    AgentThreadSummary:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
        preview:
          type: string
        messages:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              role:
                type: string
                enum:
                  - user
                  - assistant
              content:
                type: string
              createdAt:
                type: string
              interesting:
                type: array
                items:
                  type: string
                default: []
              ideas:
                type: array
                items:
                  type: object
                  properties:
                    title:
                      type: string
                    thesis:
                      type: string
                    disposition:
                      type: string
                    findings:
                      type: array
                      items:
                        type: string
                      default: []
                  required:
                    - title
                    - thesis
                default: []
              toolsUsed:
                type: array
                items:
                  type: string
                default: []
              sources:
                type: array
                items:
                  type: object
                  properties:
                    label:
                      type: string
                    url:
                      type: string
                      format: uri
                  required:
                    - label
                default: []
              chartTickers:
                type: array
                items:
                  type: string
                default: []
              mode:
                type: string
              error:
                type: string
            required:
              - id
              - role
              - content
              - createdAt
        safety:
          type: object
          properties:
            liveTradingForbidden:
              type: boolean
            orderSubmissionAllowed:
              type: boolean
          required:
            - liveTradingForbidden
            - orderSubmissionAllowed
        sourceUrl:
          type: string
          format: uri
      required:
        - id
        - title
        - preview
    ResearchArticle:
      type: object
      properties:
        id:
          type: string
        role:
          type: string
          enum:
            - user
            - assistant
        content:
          type: string
        createdAt:
          type: string
        interesting:
          type: array
          items:
            type: string
          default: []
        ideas:
          type: array
          items:
            type: object
            properties:
              title:
                type: string
              thesis:
                type: string
              disposition:
                type: string
              findings:
                type: array
                items:
                  type: string
                default: []
            required:
              - title
              - thesis
          default: []
        toolsUsed:
          type: array
          items:
            type: string
          default: []
        sources:
          type: array
          items:
            type: object
            properties:
              label:
                type: string
              url:
                type: string
                format: uri
            required:
              - label
          default: []
        chartTickers:
          type: array
          items:
            type: string
          default: []
        mode:
          type: string
        error:
          type: string
      required:
        - id
        - role
        - content
        - createdAt
    User:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        createdAt:
          type: string
        scopes:
          type: array
          items:
            type: string
            enum:
              - user
              - admin
          default:
            - user
      required:
        - id
        - email
        - createdAt
    Session:
      type: object
      properties:
        token:
          type: string
        userId:
          type: string
        expiresAt:
          type: string
      required:
        - token
        - userId
        - expiresAt
    Plan:
      type: string
      enum:
        - none
        - free_trial
        - free_forever
        - subscribed
    EntitlementState:
      type: object
      properties:
        plan:
          type: string
          enum:
            - none
            - free_trial
            - free_forever
            - subscribed
        remaining:
          type: integer
          minimum: 0
        limit:
          type: integer
          minimum: 0
        freeForever:
          type: boolean
        subscribed:
          type: boolean
        canGenerate:
          type: boolean
        canPersist:
          type: boolean
      required:
        - plan
        - remaining
        - limit
        - freeForever
        - subscribed
        - canGenerate
        - canPersist
    QuotaExceeded:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
          enum:
            - quota_exceeded
        remaining:
          type: integer
          minimum: 0
        limit:
          type: integer
          minimum: 0
        priceUsd:
          type: number
        interval:
          type: string
          enum:
            - month
      required:
        - error
        - code
        - remaining
        - limit
    BillingPlatform:
      type: string
      enum:
        - web
        - ios
        - android
    BillingChannel:
      type: string
      enum:
        - stripe
        - apple_iap
        - google_play
    BillingCheckoutRequest:
      type: object
      properties:
        platform:
          type: string
          enum:
            - web
            - ios
            - android
          default: web
        successUrl:
          type: string
        cancelUrl:
          type: string
    BillingCheckoutResponse:
      type: object
      properties:
        channel:
          type: string
          enum:
            - stripe
            - apple_iap
            - google_play
        url:
          type: string
          format: uri
        productId:
          type: string
        priceUsd:
          type: number
        interval:
          type: string
          enum:
            - month
      required:
        - channel
        - priceUsd
        - interval
    BillingPortalResponse:
      type: object
      properties:
        url:
          type: string
          format: uri
      required:
        - url
    BillingAppleRequest:
      type: object
      properties:
        signedTransaction:
          type: string
          minLength: 32
          maxLength: 16384
      required:
        - signedTransaction
    UserProgress:
      type: object
      properties:
        xp:
          type: number
        level:
          type: number
        streakDays:
          type: number
        streakFreezes:
          type: number
        lastFindDay:
          type: string
        badges:
          type: array
          items:
            type: string
          default: []
        updatedAt:
          type: string
      required:
        - xp
        - level
        - streakDays
        - streakFreezes
        - updatedAt
    ProgressResponse:
      type: object
      properties:
        progress:
          type: object
          properties:
            xp:
              type: number
            level:
              type: number
            streakDays:
              type: number
            streakFreezes:
              type: number
            lastFindDay:
              type: string
            badges:
              type: array
              items:
                type: string
              default: []
            updatedAt:
              type: string
          required:
            - xp
            - level
            - streakDays
            - streakFreezes
            - updatedAt
      required:
        - progress
    UniverseSummary:
      type: object
      properties:
        findCount:
          type: number
        valuedFinds:
          type: number
        hypotheticalBasis:
          type: number
        hypotheticalValue:
          type: number
        changePct:
          type: number
        generatedAt:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - findCount
        - valuedFinds
        - hypotheticalBasis
        - hypotheticalValue
        - changePct
        - generatedAt
        - sources
    DexRarity:
      type: string
      enum:
        - common
        - uncommon
        - rare
        - legendary
    DexRarityCounts:
      type: object
      properties:
        common:
          type: number
        uncommon:
          type: number
        rare:
          type: number
        legendary:
          type: number
      required:
        - common
        - uncommon
        - rare
        - legendary
    DexSector:
      type: object
      properties:
        sector:
          type: string
        found:
          type: number
        total:
          type: number
      required:
        - sector
        - found
        - total
    DexResponse:
      type: object
      properties:
        sectors:
          type: array
          items:
            type: object
            properties:
              sector:
                type: string
              found:
                type: number
              total:
                type: number
            required:
              - sector
              - found
              - total
        tilesVisited:
          type: number
        totalFinds:
          type: number
        rarityCounts:
          type: object
          properties:
            common:
              type: number
            uncommon:
              type: number
            rare:
              type: number
            legendary:
              type: number
          required:
            - common
            - uncommon
            - rare
            - legendary
      required:
        - sectors
        - tilesVisited
        - totalFinds
        - rarityCounts
    CompanyEdgeType:
      type: string
      enum:
        - supplies
        - buys_from
        - competes_with
        - complements
    CompanyEdge:
      type: object
      properties:
        id:
          type: string
        srcTicker:
          type: string
        dstTicker:
          type: string
        dstName:
          type: string
        edgeType:
          type: string
          enum:
            - supplies
            - buys_from
            - competes_with
            - complements
        weight:
          type: number
          minimum: 0
          maximum: 1
        reasoning:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
        asOf:
          type: string
        createdAt:
          type: string
      required:
        - id
        - srcTicker
        - dstName
        - edgeType
        - weight
        - reasoning
        - sources
        - createdAt
    CompanyGraphResponse:
      type: object
      properties:
        ticker:
          type: string
        edges:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              srcTicker:
                type: string
              dstTicker:
                type: string
              dstName:
                type: string
              edgeType:
                type: string
                enum:
                  - supplies
                  - buys_from
                  - competes_with
                  - complements
              weight:
                type: number
                minimum: 0
                maximum: 1
              reasoning:
                type: string
              sources:
                type: array
                items:
                  type: object
                  properties:
                    provider:
                      type: string
                      enum:
                        - exa
                        - openrouter
                        - gemini
                        - massive
                        - yahoo
                        - polygon
                        - sec
                        - fred
                        - manual
                    url:
                      type: string
                      format: uri
                    fetchedAt:
                      type: string
                    confidence:
                      type: string
                      enum:
                        - high
                        - medium
                        - low
                  required:
                    - provider
                    - fetchedAt
                    - confidence
              asOf:
                type: string
              createdAt:
                type: string
            required:
              - id
              - srcTicker
              - dstName
              - edgeType
              - weight
              - reasoning
              - sources
              - createdAt
        count:
          type: number
        generatedAt:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - ticker
        - edges
        - count
        - generatedAt
        - sources
    QuestKind:
      type: string
      enum:
        - catch_any
        - catch_private
        - new_tile
        - new_sector
    Quest:
      type: object
      properties:
        id:
          type: string
        kind:
          type: string
          enum:
            - catch_any
            - catch_private
            - new_tile
            - new_sector
        title:
          type: string
        xp:
          type: number
        completed:
          type: boolean
        progress:
          type: number
        target:
          type: number
      required:
        - id
        - kind
        - title
        - xp
        - completed
        - progress
        - target
    QuestsResponse:
      type: object
      properties:
        quests:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              kind:
                type: string
                enum:
                  - catch_any
                  - catch_private
                  - new_tile
                  - new_sector
              title:
                type: string
              xp:
                type: number
              completed:
                type: boolean
              progress:
                type: number
              target:
                type: number
            required:
              - id
              - kind
              - title
              - xp
              - completed
              - progress
              - target
        day:
          type: string
        xpGrantedToday:
          type: number
      required:
        - quests
        - day
        - xpGrantedToday
    TerritoryResponse:
      type: object
      properties:
        tile:
          type: string
        investablesTotal:
          type: number
        found:
          type: number
        pioneer:
          type: boolean
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - tile
        - investablesTotal
        - found
        - pioneer
        - sources
    ActiveEvent:
      type: object
      properties:
        key:
          type: string
        title:
          type: string
        sector:
          type: string
        multiplier:
          type: number
        startsAt:
          type: string
        endsAt:
          type: string
      required:
        - key
        - title
        - multiplier
        - startsAt
        - endsAt
    EventsResponse:
      type: object
      properties:
        active:
          type:
            - object
            - "null"
          properties:
            key:
              type: string
            title:
              type: string
            sector:
              type: string
            multiplier:
              type: number
            startsAt:
              type: string
            endsAt:
              type: string
          required:
            - key
            - title
            - multiplier
            - startsAt
            - endsAt
      required:
        - active
    Rivalry:
      type: object
      properties:
        id:
          type: string
        ticker:
          type: string
        rivalTicker:
          type: string
        wins:
          type: number
        losses:
          type: number
        draws:
          type: number
        currentPick:
          type: string
          enum:
            - ticker
            - rival
        weekStart:
          type: string
        createdAt:
          type: string
      required:
        - id
        - ticker
        - rivalTicker
        - wins
        - losses
        - draws
        - weekStart
        - createdAt
    RivalriesResponse:
      type: object
      properties:
        rivalries:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              ticker:
                type: string
              rivalTicker:
                type: string
              wins:
                type: number
              losses:
                type: number
              draws:
                type: number
              currentPick:
                type: string
                enum:
                  - ticker
                  - rival
              weekStart:
                type: string
              createdAt:
                type: string
            required:
              - id
              - ticker
              - rivalTicker
              - wins
              - losses
              - draws
              - weekStart
              - createdAt
        count:
          type: number
      required:
        - rivalries
        - count
    CreateRivalryRequest:
      type: object
      properties:
        ticker:
          type: string
        rivalTicker:
          type: string
      required:
        - ticker
    DemandPulseBuyer:
      type: object
      properties:
        ticker:
          type: string
        name:
          type: string
        revenueYoY:
          type: number
        capexYoY:
          type: number
        weight:
          type: number
          minimum: 0
          maximum: 1
      required:
        - ticker
        - weight
    DemandPulse:
      type: object
      properties:
        ticker:
          type: string
        buyers:
          type: array
          items:
            type: object
            properties:
              ticker:
                type: string
              name:
                type: string
              revenueYoY:
                type: number
              capexYoY:
                type: number
              weight:
                type: number
                minimum: 0
                maximum: 1
            required:
              - ticker
              - weight
        pulse:
          type:
            - number
            - "null"
        interpretation:
          type: string
          enum:
            - expanding
            - contracting
            - mixed
            - unknown
        generatedAt:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - ticker
        - buyers
        - pulse
        - interpretation
        - generatedAt
        - sources
    EnvironmentSeries:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        latest:
          type: number
        unit:
          type: string
        asOf:
          type: string
      required:
        - id
        - label
        - latest
        - asOf
    EnvironmentBrief:
      type: object
      properties:
        sector:
          type: string
        headline:
          type: string
        body:
          type: string
        tailwinds:
          type: array
          items:
            type: string
        headwinds:
          type: array
          items:
            type: string
        series:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              label:
                type: string
              latest:
                type: number
              unit:
                type: string
              asOf:
                type: string
            required:
              - id
              - label
              - latest
              - asOf
        generatedAt:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - sector
        - headline
        - body
        - tailwinds
        - headwinds
        - series
        - generatedAt
        - sources
    SynthesisMemoResponse:
      type: object
      properties:
        ticker:
          type: string
        memo:
          type: string
        bindingConstraint:
          type: string
        demandDurability:
          type: string
        pricingPower:
          type: string
        generatedAt:
          type: string
        sources:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                  - exa
                  - openrouter
                  - gemini
                  - massive
                  - yahoo
                  - polygon
                  - sec
                  - fred
                  - manual
              url:
                type: string
                format: uri
              fetchedAt:
                type: string
              confidence:
                type: string
                enum:
                  - high
                  - medium
                  - low
            required:
              - provider
              - fetchedAt
              - confidence
      required:
        - ticker
        - memo
        - generatedAt
        - sources
    ErrorResponse:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details: {}
          required:
            - code
            - message
      required:
        - error
  parameters: {}
paths:
  /v1/health:
    get:
      summary: Health check
      description: Liveness probe. Returns 200 while the API is accepting traffic.
      tags:
        - system
      responses:
        "200":
          description: Service is healthy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ok
                  uptime:
                    type: number
                    description: Seconds since process start
                required:
                  - status
                  - uptime
  /v1/identify:
    post:
      summary: Identify investable brands from a photo
      description: Accepts a multipart image plus optional location. Runs the vision pipeline and returns detected brands with ticker / comparable / ETF annotations. See `packages/vision`.
      tags:
        - identify
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                image:
                  type: string
                  description: Photo bytes (JPEG or PNG).
                  format: binary
                location:
                  allOf:
                    - $ref: "#/components/schemas/LatLng"
                    - description: Optional device location to bias identification.
              required:
                - image
      responses:
        "200":
          description: Identification succeeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentifyResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/finds:
    get:
      summary: Finds journal for the signed-in user
      description: Every successful `/v1/identify` by a signed-in user records the top investable as a find. Returns finds newest-first.
      tags:
        - identify
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type:
              - number
              - "null"
            description: Max finds to return. Default 100, capped at 200.
            example: 100
          required: false
          description: Max finds to return. Default 100, capped at 200.
          name: limit
          in: query
      responses:
        "200":
          description: Finds returned newest-first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FindsResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/progress:
    get:
      summary: Progression (XP, level, streak) for the signed-in user
      description: Server-side truth for XP, level, streak length, streak-freeze inventory, and earned `badges`. Written by `recordFind` on every successful `/v1/identify`; the client renders this rather than deriving a streak locally, so the streak survives reinstall. `lastFindDay` is a UTC calendar day (`YYYY-MM-DD`). `badges` holds opaque earned badge keys (e.g. `sector:Consumer Staples`) awarded server-side — clients never post a badge and must render an unknown key generically.
      tags:
        - identify
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Progression state returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProgressResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/universe/summary:
    get:
      summary: Counterfactual universe portfolio
      description: "The `$100 per find at found price` aggregate: what the caller's universe would be worth if $100 had gone into every find at the moment it was found. Computed from each find's `foundPrice` against the current quote. Finds without a `foundPrice` are excluded from the valued totals, never estimated (AGENTS.md §2.4). Hypothetical only — not a holdings statement. `sources` cites each quote provider that actually declared itself, with confidence derived from the quote's own freshness declaration (AGENTS.md §6 — a quote reporting no provider adds no citation rather than a fabricated one)."
      tags:
        - identify
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Universe summary computed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UniverseSummary"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/dex:
    get:
      summary: Collection progress (sector dexes + regional tiles)
      description: "Derived on read by reconciling the caller's finds against the `brands.json` seed in `packages/finance`: per-sector found/total counts, plus `tilesVisited` (distinct geohash-6 tiles with at least one find) and `rarityCounts`, the per-find histogram over the four `DexRarity` tiers. Every find is classified into exactly one tier, so the four counts sum to `totalFinds`."
      tags:
        - identify
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dex progress returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DexResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/quests:
    get:
      summary: Daily quests for the signed-in user
      description: The caller's quest set for the current UTC day. Quests cover verifiable actions only (`catch_any`, `catch_private`, `new_tile`, `new_sector`) and are evaluated server-side from the find stream — completion is never self-reported, so there is no endpoint to claim one. `id` is deterministic per day and kind (`{YYYY-MM-DD}:{kind}`), which is what makes XP grantable at most once per quest. `xpGrantedToday` reports XP already written to `user_progress` for `day`; the client displays it and never adds to it.
      tags:
        - identify
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Quest set for the current UTC day.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuestsResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/territory:
    get:
      summary: Tile completion for a location
      description: Completion state for the geohash-6 tile containing `lat`/`lng`. `investablesTotal` is how many investable brands the same nearby cascade behind `/v1/nearby` resolves inside the tile, and `found` is how many of those the caller has already caught, so the two reconcile with `/v1/nearby` for that tile. `pioneer` is true when the caller has not yet recorded a find in this tile — the write-time pioneer XP bonus is still available here. The counts come from a live places + brand join, so the response carries `sources` (AGENTS.md §6) — an uncitable lookup returns fewer sources, never an invented one.
      tags:
        - identify
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type:
              - number
              - "null"
            minimum: -90
            maximum: 90
            example: 40.7128
          required: false
          name: lat
          in: query
        - schema:
            type:
              - number
              - "null"
            minimum: -180
            maximum: 180
            example: -74.006
          required: false
          name: lng
          in: query
      responses:
        "200":
          description: Tile completion resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TerritoryResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/events/current:
    get:
      summary: Currently open XP event, if any
      description: The scheduler-driven quest modifier that is open right now — e.g. Sector Saturday doubling XP for one sector, or an earnings-week window. The schedule is global rather than per-user, so no auth is required. `active` is explicitly `null` when no window is open, letting the client distinguish 'no event' from a failed fetch. The `multiplier` applies to find XP only (quest XP is awarded at face value), only inside `[startsAt, endsAt)`, and an event with no `sector` applies to every sector.
      tags:
        - identify
      responses:
        "200":
          description: Active event, or `null` when no window is open.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventsResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/rivalries:
    get:
      summary: Weekly matchups for the signed-in user
      description: "The caller's tracked solo matchups (e.g. NVDA vs AMD) with the running win/loss/draw record. Collection and comprehension mechanic only: a rivalry is not a position and the copy never instructs a trade. `weekStart` is the Monday of the current round as a UTC calendar day, so the round boundary does not move with the device timezone."
      tags:
        - identify
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Rivalries returned newest-first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RivalriesResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Start a weekly matchup against a comparable
      description: Creates a solo weekly matchup for one of the caller's finds. Omit `rivalTicker` and the server picks the opponent from the existing comparables pipeline in `packages/finance` — the client must not invent a rival, and a ticker with no resolvable comparable returns 422 rather than a fabricated opponent. The optional pre-registered `currentPick` on the returned rivalry is a conviction game scored for XP; it is not an order and creates no position.
      tags:
        - identify
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRivalryRequest"
      responses:
        "201":
          description: Rivalry created; the full updated list is returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RivalriesResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "409":
          description: A rivalry for this exact pairing already exists.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "422":
          description: No comparable could be resolved for `ticker` — pass an explicit `rivalTicker` instead.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/rivalries/{id}/pick:
    post:
      summary: Pre-register (or clear) the conviction pick for the open round
      description: 'Body is `{ pick: "ticker" | "rival" | null }` — `null` clears the pick. A correct pick earns XP at the weekly close, which then clears it so a pick never carries into a round it was not registered for. Not an order; creates no position.'
      tags:
        - identify
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                pick:
                  type:
                    - string
                    - "null"
                  enum:
                    - ticker
                    - rival
              required:
                - pick
      responses:
        "200":
          description: Pick recorded; the full updated list is returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RivalriesResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "404":
          description: Rivalry not found (or not the caller's).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/rivalries/{id}:
    delete:
      summary: Delete a rivalry
      tags:
        - identify
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "204":
          description: Rivalry deleted.
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "404":
          description: Rivalry not found (or not the caller's).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/nearby:
    get:
      summary: Nearby investable places
      description: "Google Places lookup around a lat/lng, filtered and annotated with tickers/ETFs. Every finance answer includes a `sources: Source[]` array (see AGENTS.md §6)."
      tags:
        - nearby
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type: number
          required: true
          name: lat
          in: query
        - schema:
            type: number
          required: true
          name: lng
          in: query
        - schema:
            type: number
            default: 500
          required: false
          name: radius
          in: query
        - schema:
            type: number
            default: 25
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Nearby items resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NearbyResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/widget/nearby:
    get:
      summary: Trimmed nearby payload for home-screen widgets
      description: Same places cascade + brand join as `/v1/nearby`, capped small (max 12) with quotes attached for up to 6 tickers. Built for the iOS WidgetKit and Android home-screen widgets, which refresh on a timeline rather than on demand. No auth required.
      tags:
        - widget
      parameters:
        - schema:
            type: number
          required: true
          name: lat
          in: query
        - schema:
            type: number
          required: true
          name: lng
          in: query
        - schema:
            type: number
            default: 500
          required: false
          name: radius
          in: query
        - schema:
            type: number
            default: 25
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Widget-sized nearby items resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WidgetNearbyResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/widget/map-snapshot:
    get:
      summary: Static map PNG for home-screen widgets
      description: Server-rendered Google Static Maps PNG with a pin for the origin and one per nearby investable, labeled by ticker. Proxies the request so `GOOGLE_MAPS_API_KEY` never reaches a widget extension (see docs/SECRETS.md). Returns 501 when no key is configured.
      tags:
        - widget
      parameters:
        - schema:
            type: number
          required: true
          name: lat
          in: query
        - schema:
            type: number
          required: true
          name: lng
          in: query
        - schema:
            type: number
            default: 500
          required: false
          name: radius
          in: query
        - schema:
            type: number
            default: 25
          required: false
          name: limit
          in: query
        - schema:
            type:
              - number
              - "null"
            description: PNG width in px, capped at 640.
          required: false
          description: PNG width in px, capped at 640.
          name: width
          in: query
        - schema:
            type:
              - number
              - "null"
            description: PNG height in px, capped at 640.
          required: false
          description: PNG height in px, capped at 640.
          name: height
          in: query
      responses:
        "200":
          description: Map snapshot PNG.
          content:
            image/png:
              schema:
                type: string
                format: binary
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Map snapshot not configured (GOOGLE_MAPS_API_KEY unset).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Upstream places lookup or static map request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/resolve-comparable:
    post:
      summary: Resolve a private brand to a public comparable + ETF exposure
      description: Given a brand name, returns the closest public comparable, ETF exposures, and cited sources. Confidence is `low` when the pipeline could not cite a source (do not fabricate).
      tags:
        - finance
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveComparableRequest"
      responses:
        "200":
          description: Comparable resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResolveComparableResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/quote-history:
    get:
      summary: Daily provider-routed price history
      description: Daily adjusted closes from the configured market-data provider for the native Overview price series. Period defaults to `1mo`. Does not invent prices — 502 when history is unavailable.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: AAPL
          required: true
          name: symbol
          in: query
        - schema:
            type: string
            enum:
              - 1mo
              - 3mo
              - 6mo
              - 1y
            example: 1mo
          required: false
          name: period
          in: query
      responses:
        "200":
          description: Daily close series with provider source citation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteHistoryResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Market-data history unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/chart/{type}:
    get:
      summary: Underlying Analyzer chart image
      description: Proxies `POST /api/charts/{type}` on Underlying Analyzer. Period aliases `1m`/`1M` normalize to the sibling analyzer's `1mo` period.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: auction
          required: true
          name: type
          in: path
        - schema:
            type: string
            example: MCD
          required: true
          name: ticker
          in: query
        - schema:
            type: string
            example: 1mo
          required: false
          name: period
          in: query
        - schema:
            type:
              - number
              - "null"
          required: false
          name: month
          in: query
      responses:
        "200":
          description: Chart PNG as base64 plus optional auction levels.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChartResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/market-data/capabilities:
    get:
      summary: Market-data provider capabilities
      description: Returns the configured primary provider, freshness declaration, subscription labels, and dataset capability notes. The subscription labels are operational configuration and are not inferred from API responses.
      tags:
        - finance
      responses:
        "200":
          description: Provider capability report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketDataCapabilities"
  /v1/market-data/aggregates:
    get:
      summary: Historical market aggregates
      description: Additive OHLCV aggregate endpoint backed by Massive when configured. Dates are YYYY-MM-DD; timestamps in points are Unix seconds. The endpoint returns an opaque `nextCursor` when the provider has another page.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: AAPL
          required: true
          name: symbol
          in: query
        - schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            example: 2026-01-01
          required: false
          name: from
          in: query
        - schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            example: 2026-01-31
          required: false
          name: to
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 1000
            example: 1
          required: false
          name: multiplier
          in: query
        - schema:
            type: string
            enum:
              - minute
              - hour
              - day
              - week
              - month
              - quarter
              - year
            example: day
          required: false
          name: timespan
          in: query
        - schema:
            type:
              - boolean
              - "null"
          required: false
          name: adjusted
          in: query
        - schema:
            type: string
            enum:
              - stocks
              - options
          required: false
          name: assetClass
          in: query
      responses:
        "200":
          description: Aggregate bars.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AggregatesResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Market-data aggregates unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/financials/ratios:
    get:
      summary: Financial ratios
      description: Additive financial-ratios page backed by the configured market-data provider. Continuation cursors are opaque and must be passed back unchanged.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            pattern: ^[A-Z][A-Z0-9._-]{0,14}$
          required: true
          name: ticker
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 1
          required: false
          name: limit
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 2048
            pattern: ^[^\s]+$
          required: false
          name: cursor
          in: query
      responses:
        "200":
          description: Financial ratios page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialRatiosResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Financial ratios are unsupported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Financial ratios unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/options/summary:
    get:
      summary: Option contract summary
      description: Additive single-contract snapshot backed by Massive options data, including normalized contract details, greeks, quote, trade, and day fields when available.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            pattern: ^[A-Z][A-Z0-9._-]{0,14}$
          required: true
          name: underlying
          in: query
        - schema:
            type: string
            pattern: ^O:[A-Z0-9._-]{1,48}$
          required: true
          name: contract
          in: query
      responses:
        "200":
          description: Option summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptionSummaryResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "404":
          description: Option summary not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Option snapshots are unsupported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Option summary unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/options/bars:
    get:
      summary: Option contract bars
      description: Additive OHLCV bars for an options contract. The provider continuation cursor is opaque and must be passed back unchanged.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            pattern: ^O:[A-Z0-9._-]{1,48}$
          required: true
          name: ticker
          in: query
        - schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: true
          name: from
          in: query
        - schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: true
          name: to
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
          required: false
          name: multiplier
          in: query
        - schema:
            type: string
            enum:
              - minute
              - hour
              - day
              - week
              - month
              - quarter
              - year
            default: day
          required: false
          name: timespan
          in: query
        - schema:
            type: boolean
            default: true
          required: false
          name: adjusted
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 2048
            pattern: ^[^\s]+$
          required: false
          name: cursor
          in: query
      responses:
        "200":
          description: Option aggregate bars page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptionBarsResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Option aggregates are unsupported.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Option bars unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/options/chain:
    get:
      summary: Options chain snapshot
      description: Additive option-chain snapshot with Massive-normalized contracts, greeks, implied volatility, quotes, trades, and open interest when the subscribed Options plan includes them.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: AAPL
          required: true
          name: underlying
          in: query
        - schema:
            type: string
          required: false
          name: expiration_date
          in: query
        - schema:
            type: string
            enum:
              - call
              - put
          required: false
          name: contract_type
          in: query
        - schema:
            type:
              - number
              - "null"
          required: false
          name: strike_price
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 250
          required: false
          name: limit
          in: query
        - schema:
            type: string
          required: false
          name: cursor
          in: query
      responses:
        "200":
          description: Options chain page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptionsResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Options chain unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/options/contracts:
    get:
      summary: Options contract reference data
      description: Additive, cursor-paginated options contract index. Massive exposes active and expired contracts with expiration dates, strikes, types, and exercise styles.
      tags:
        - finance
      parameters:
        - schema:
            type: string
          required: false
          name: underlying
          in: query
        - schema:
            type: string
          required: false
          name: ticker
          in: query
        - schema:
            type: string
          required: false
          name: expiration_date
          in: query
        - schema:
            type: string
            enum:
              - call
              - put
          required: false
          name: contract_type
          in: query
        - schema:
            type:
              - boolean
              - "null"
          required: false
          name: expired
          in: query
        - schema:
            type: string
          required: false
          name: as_of
          in: query
        - schema:
            type:
              - number
              - "null"
          required: false
          name: strike_price
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 1000
          required: false
          name: limit
          in: query
        - schema:
            type: string
          required: false
          name: cursor
          in: query
      responses:
        "200":
          description: Options contracts page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptionContractsResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Options contracts unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/options/contracts/{ticker}:
    get:
      summary: Single options contract
      description: Additive options contract reference lookup by Massive options ticker.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: O:AAPL260116C00100000
          required: true
          name: ticker
          in: path
      responses:
        "200":
          description: Options contract.
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract:
                    $ref: "#/components/schemas/OptionContract"
                  sources:
                    type: array
                    items:
                      $ref: "#/components/schemas/Source"
                required:
                  - contract
                  - sources
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Options contract unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/market-events:
    get:
      summary: Corporate actions and market events
      description: Returns Massive stock splits and dividends, plus optional TMX/Wall Street Horizon corporate events when the partner dataset is enabled.
      tags:
        - finance
      parameters:
        - schema:
            type: string
          required: false
          name: ticker
          in: query
        - schema:
            type: string
          required: false
          name: from
          in: query
        - schema:
            type: string
          required: false
          name: to
          in: query
        - schema:
            type:
              - number
              - "null"
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Market events.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketEventsResponse"
        "400":
          description: Bad request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Market-data rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Market events unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/analysis/{ticker}:
    get:
      summary: Underlying Analyzer snapshot
      description: Lightweight stock summary (+ brief when present). Full Anthropic brief via POST /v1/memo.
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: MCD
          required: true
          name: ticker
          in: path
      responses:
        "200":
          description: Analysis snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnalysisSnapshot"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/graph/{ticker}:
    get:
      summary: Company value-chain graph
      description: "Cache-first: served from the `company_edges` store, extracted on miss with in-flight dedupe per ticker and refreshed when a new 10-K lands rather than on a short TTL. Cache hits are free and identity-less; a cache MISS spends provider money, so generation requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent; concurrent callers sharing one in-flight extraction are charged once). Edges are cited from 10-K items 1/1A evidence (supplier concentration and >10% customers are disclosed there) plus Exa open-web results; every edge carries `sources` (AGENTS.md §6) and an edge that cannot be cited is not emitted. Private counterparties keep `dstName` with no `dstTicker` — never an invented one. Empty and failing extractions are negatively cached for an hour. Returns 502 when extraction fails."
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: NVDA
          required: true
          name: ticker
          in: path
      responses:
        "200":
          description: Graph edges returned (cache hit or fresh extraction).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanyGraphResponse"
        "400":
          description: Bad request — malformed ticker, or a cache-miss generation attempted with neither a bearer session nor an `X-Device-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Graph extraction failed (filings or Exa evidence unavailable).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/pulse/{ticker}:
    get:
      summary: Demand pulse (is the money upstream growing or shrinking)
      description: "Aggregates the buyer side of a ticker's value chain into one signal: the `buys_from` counterparties from `/v1/graph/{ticker}` joined to provider income-statement / cash-flow series, weighted by edge weight. Metered exactly like `/v1/graph` — cache hits are free and identity-less; a cache MISS spends provider money, so generation requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent). `pulse` is `null` (and `interpretation` is `unknown`) when no buyer fundamentals resolved — never zero-filled — and a buyer whose series is missing keeps `revenueYoY`/`capexYoY` omitted (AGENTS.md §2.4). Every fetched series is cited in `sources` (AGENTS.md §6)."
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: NVDA
          required: true
          name: ticker
          in: path
      responses:
        "200":
          description: Demand pulse returned (cache hit or fresh computation).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DemandPulse"
        "400":
          description: Bad request — malformed ticker, or a cache-miss generation attempted with neither a bearer session nor an `X-Device-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Demand pulse unavailable (graph edges or fundamentals unreachable).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Market-data provider is not configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/environment/{sector}:
    get:
      summary: Macro environment brief for a sector
      description: "The gather → LLM → tailwinds/headwinds brief at sector scale, cached 24h. `series` holds the quantitative FRED observations behind the brief; policy and culture color gathered via Exa with recency filters is qualitative and is cited in `sources` alongside them, never promoted into `series`. Metered exactly like `/v1/graph` — cache hits are free, a cache MISS requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent). Degrades honestly: with `FRED_API_KEY` unset the brief ships with `series: []` and Exa color only; 503 only when neither FRED nor Exa (nor the LLM) is configured. Macro numbers are never synthesized."
      tags:
        - finance
      parameters:
        - schema:
            type: string
            example: Consumer Staples
          required: true
          name: sector
          in: path
      responses:
        "200":
          description: Environment brief returned (cache hit or fresh generation).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentBrief"
        "400":
          description: Bad request — unknown sector, or a cache-miss generation attempted with neither a bearer session nor an `X-Device-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Environment brief generation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Environment layer not configured (neither FRED_API_KEY nor EXA_API_KEY, or no LLM key).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/memo/synthesis:
    post:
      summary: Layered synthesis memo (billable)
      description: "The layered memo. The prompt receives the three layer briefs — upstream (`/v1/graph/{ticker}`), demand (`/v1/pulse/{ticker}`), and environment (`/v1/environment/{sector}`) — plus ratios, and is asked exactly three questions: what is the binding constraint on this business, how durable is the demand above it, and where in the chain does pricing power sit. Those answers come back as `bindingConstraint` / `demandDurability` / `pricingPower`, each grounded in the cited layer briefs. They are optional because the memo degrades gracefully: with an empty graph, no pulse, and no environment brief it returns a plain `memo` and omits the layer answers rather than guessing them. Metered exactly like `/v1/graph` — cache hits are free, a cache MISS requires a bearer session or `X-Device-Id` and counts against the free-tier generation quota (402 when spent). Every layer fact carried into an answer is cited in `sources` (AGENTS.md §6)."
      tags:
        - finance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ticker:
                  type: string
                  example: NVDA
              required:
                - ticker
      responses:
        "200":
          description: Synthesis memo generated (cache hit or fresh generation).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SynthesisMemoResponse"
        "400":
          description: Bad request — malformed ticker, or a cache-miss generation attempted with neither a bearer session nor an `X-Device-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Memo generation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /v1/cockpit:
    post:
      summary: Watchlist cockpit (Underlying Analyzer)
      description: Batch rank for up to 10 tickers via POST /api/watchlists/cockpit.
      tags:
        - finance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tickers:
                  type: array
                  items:
                    type: string
                  minItems: 1
                  maxItems: 10
              required:
                - tickers
      responses:
        "200":
          description: Ranked cockpit rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CockpitResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/alerts:
    post:
      summary: Watchlist alerts digest (Underlying Analyzer)
      description: Alert digest for up to 10 tickers via POST /api/watchlists/alerts.
      tags:
        - finance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tickers:
                  type: array
                  items:
                    type: string
                  minItems: 1
                  maxItems: 10
                maxAlerts:
                  type: number
              required:
                - tickers
      responses:
        "200":
          description: Alerts digest.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlertsResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/agent/chat:
    post:
      summary: Ticker-bound research brief (Derivation idea-chats)
      description: "Aggregates Derivation Research Console SSE into one article-shaped assistant turn. Product IA: open from ticker Research…; history under Saved → Briefs. Factory/Jobs UI not exposed. Broker orders off."
      tags:
        - finance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentChatRequest"
      responses:
        "200":
          description: Research article + optional thread id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentChatResponse"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/agent/threads:
    get:
      summary: List persisted research brief threads
      description: Proxies Derivation GET /api/idea-chats. Shown under Saved → Briefs.
      tags:
        - finance
      responses:
        "200":
          description: Thread summaries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  threads:
                    type: array
                    items:
                      $ref: "#/components/schemas/AgentThreadSummary"
                  count:
                    type: number
                  sourceUrl:
                    type: string
                    format: uri
                required:
                  - threads
                  - count
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/agent/threads/{id}:
    get:
      summary: Get one research brief thread
      tags:
        - finance
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Full thread with messages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thread:
                    $ref: "#/components/schemas/AgentThreadSummary"
                required:
                  - thread
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/auth/session:
    post:
      summary: Passwordless email sign-in (magic link)
      description: Starts or completes a magic-link session. On first call with `{email}`, an email is dispatched. On second call with `{email, code}`, a session token is returned.
      tags:
        - auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                code:
                  type: string
                  description: Magic-link code from the email. Omit on the first call to trigger send.
              required:
                - email
      responses:
        "200":
          description: Session issued or magic-link dispatched.
          content:
            application/json:
              schema:
                anyOf:
                  - type: object
                    properties:
                      sent:
                        type: boolean
                        enum:
                          - true
                    required:
                      - sent
                  - type: object
                    properties:
                      session:
                        $ref: "#/components/schemas/Session"
                      user:
                        $ref: "#/components/schemas/User"
                    required:
                      - session
                      - user
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/entitlements:
    get:
      summary: Free-tier remaining + plan
      description: Auth optional. Anonymous callers send `X-Device-Id` so remaining generations can be tracked per device. Mirrors the state `requireGenerationQuota` consumes.
      tags:
        - billing
      responses:
        "200":
          description: Current entitlement state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntitlementState"
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/billing/checkout:
    post:
      summary: Start a $19.99/mo subscription
      description: "Signed-in only. `platform` selects the charge channel: Stripe Checkout URL for web (and iOS/Android until native product ids are configured), or `apple_iap` / `google_play` product ids once those env vars are set. Clients must not invent payment URLs."
      tags:
        - billing
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillingCheckoutRequest"
      responses:
        "200":
          description: Checkout intent for the resolved channel.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingCheckoutResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Billing not configured (Stripe keys or native product id missing).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/billing/portal:
    post:
      summary: Stripe customer portal (manage / cancel)
      description: Signed-in Stripe subscribers only. Native-store subscribers manage billing in App Store / Play, not here.
      tags:
        - billing
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Portal URL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingPortalResponse"
        "400":
          description: No Stripe customer on file — subscribe first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Billing not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/billing/apple:
    post:
      summary: Redeem a StoreKit 2 transaction
      description: Signed-in only. Body is the JWS from StoreKit (`purchase.purchaseToken` on iOS). The API verifies Apple's ES256 + x5c chain against Apple Root CA G3, then sets `plan=subscribed`. Native subscribers manage billing in the App Store, not Stripe portal.
      tags:
        - billing
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillingAppleRequest"
      responses:
        "200":
          description: Updated entitlement state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntitlementState"
        "400":
          description: Invalid, expired, revoked, or untrusted Apple JWS.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: This Apple transaction is already linked to another Mapvest account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/memo:
    post:
      summary: Generate a ticker memo (billable)
      description: Full research memo for a ticker. Counts against the 50-generation free tier. Returns 402 `quota_exceeded` when the meter is spent.
      tags:
        - finance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ticker:
                  type: string
              required:
                - ticker
      responses:
        "200":
          description: Memo generated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ticker:
                    type: string
                  provider:
                    type: string
                  memo:
                    type: string
                required:
                  - ticker
                  - provider
                  - memo
        "400":
          description: Bad request — validation failed against the zod schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Free-tier generation quota spent. Clients must present the paywall, not a generic error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaExceeded"
        "429":
          description: Rate limit exceeded (per-user or per-ip).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/admin/metrics:
    get:
      summary: "Admin: request + cost metrics"
      description: Requires the `admin` scope. Returns rolling counters for identify/nearby/resolve calls and OpenRouter cost telemetry.
      tags:
        - admin
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Metrics snapshot.
          content:
            application/json:
              schema:
                type: object
                properties:
                  requests:
                    type: object
                    additionalProperties:
                      type: number
                  costUsd:
                    type: number
                  windowSeconds:
                    type: number
                required:
                  - requests
                  - costUsd
                  - windowSeconds
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller lacks the `admin` scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/admin/users:
    get:
      summary: "Admin: list users"
      description: Requires the `admin` scope. Returns registered users.
      tags:
        - admin
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      $ref: "#/components/schemas/User"
                required:
                  - users
        "401":
          description: Missing or invalid session token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller lacks the `admin` scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
webhooks: {}
